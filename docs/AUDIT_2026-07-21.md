# Post-v2.2.0 audit — 2026-07-21

**Status: LIVE working board. Not an archive record.**

> **Do not move this file to `docs/archive/` until every item in it is closed or
> explicitly deleted.** `docs/archive/SWEEP_2026-07-16.md` was archived with three
> sections of *live* content still in it — in-code promise archaeology, fresh-clone
> defects, and a ranked 22-item feature slate — and `grep -c SWEEP docs/STATE.md`
> returned **0**. Because `CLAUDE.md` makes `docs/archive/` "historical record, never
> current fact", roughly twenty items became contractually invisible. This audit
> found them. Do not repeat the mechanism on this file.

## How this was produced

Sixteen parallel audit lenses — ten across the codebase, six across the documentation —
each followed by an independent adversarial reviewer whose job was to *disprove* every
`STILL_OPEN` finding by re-deriving it from source. 33 agents, no errors, against
`loom-v2 @ 984f348` (v2.2.0, clean tree, full suite green -- `python -m pytest` at that commit).

Raw totals: **227 findings** — 178 claimed open, **51 killed on refutation**, 32 already
fixed, 9 decided-intentional, 8 never true. Everything below survived a refuter.

Every claim carries a `file:line`. Where this board and any other document disagree,
**this board was checked against the code and the other document may not have been** —
but re-verify before acting, because the tree moves.

---

# Moonglade v2.2.0 — Consolidated Audit Board

Tree verified: `loom-v2` @ `984f348`, clean, v2.2.0, full suite green. (Lenses disagreed on the count; `python -m pytest` at this commit is the answer, and this file will not carry the number -- see CLAUDE.md.)

---

## 1. Did this release drop something? Yes — four things, all from the component migration and one from the Loom V1 retirement.

**The GUI removal (v2.1.0) was 99% clean.** Zero PySide6/`pixai_gui` references survive in requirements, CI, `.gitignore`, packaging, or tests. What it left: ~~three zero-caller `run_*` shims~~ **FIXED 2026-07-23** (`run_verify_dupes`/`run_audit`/`run_dedup` deleted outright, commit `a4a3de7` — the real `cmd_audit`/`cmd_dedup`/`cmd_verify_dupes` logic they wrapped is untouched; same finding as O6 below), ~~two shipped UI strings pointing at deleted GUI tabs~~ **FIXED 2026-07-22** (see P5 — `pixai_gallery.py` now has zero `\bGUI\b` matches), and one real capability gap — `--convert-existing` and `--backfill-meta` lost their only non-terminal door (confirmed still open: no web route for either) — ~~while `docs/STATE.md:446` says "Nothing" is CLI-only~~ **FIXED 2026-07-22** (see P3 — `docs/STATE.md`'s Control Panel/web-parity section, now ~line 530-546, correctly names both flags as CLI-only instead of "Nothing").

**The classic-Loom retirement (2026-07-17) lost nothing from its own punch list** — all eleven V1-only capabilities the roadmap named at risk have V2/Deep Focus homes. But it orphaned one thing nobody listed: **`frameLinked()` has zero call sites.** `loom/master-storyboard.jsx:9` imports it, `:214` names it in a comment, and nothing calls it. The continuity indicator it fixed does not exist anywhere in V2. Six green unit tests (`loom/test/loom-core.test.js:73-102`) still cover it, and `docs/STATE.md:227` lists it among live Loom logic — so the suite and the docs both report coverage of dead code.

~~**The 95-rule CSS prune (2026-07-21) targeted classic Loom and missed the Video-tab swap's residue** — `.vp-chip` and `.gen-ce` (`pixai_gallery.py:4977-4981`, 5 rules, zero producers), sitting two rules below a comment congratulating itself for deleting `.gen-cost` for exactly that reason.~~ **FIXED 2026-07-23** (commit `4d07548`): both rule sets deleted; `pixai_gallery.py` now has zero occurrences of `.gen-ce` or `.vp-chip` anywhere in the repo. See O9 in Tier 3.

**The Option A migration is where the real drop is.** Commit `2b20806` swapped the gallery's hand-rolled video form for the shared `<mg-generate-drawer>` and dropped two behaviours on the way through:

- ~~**The gallery's Video tab lost Job Tracker registration and the credit refresh.**~~
  **FIXED 2026-07-21 (B4).** `pixai_gallery.py` now also binds `mg-submit` (calls
  `Jobs.register`, mirroring the Loom's `"Rendered"` label) and `mg-result` (calls
  `Acct.refresh()`), right after the existing `mg-pick-request` listener. New test
  `tests/test_web_pick.py::test_gallery_video_tab_registers_with_the_job_tracker`,
  verified to FAIL on the pre-fix source. Original finding: `pixai_gallery.py` bound
  exactly **one** `mg-*` listener (`:6372`, `mg-pick-request`); `loom/master-storyboard.jsx`
  binds **14** on the same element. Before the swap, `videoGenerate()` went through
  `runTask()`, which calls `Jobs.track(...)` + `Acct.refresh()` (`pixai_gallery.py:6121`).
  Because `_reconcile_job` (`:3133-3136`) "never invents" a job it isn't already tracking,
  a gallery video generation that finished after you navigated away left **no Activity row
  at all**. The Loom, mounting the same element, registers correctly (`jsx:2453`).
- ~~**`bulkSendVideo` still sends 9 refs to a 6-slot bank.** `pixai_gallery.py:6498` `Gen.addVideoRefs(refs.slice(0,9))` against `:6341-6343` `refs=(refs||[]).slice(0,6)`. Select seven images, hit Send to Video, three vanish silently into a full-looking bank.~~ **FIXED 2026-07-23** (commit `4d07548`): `bulkSendVideo()` (`pixai_gallery.py:6730-6747`) no longer pre-slices to 9 before calling `Gen.addVideoRefs()` (which still caps at 6, `:6560`) — it now passes the full selection and shows a `Toast` warning ("Only 6 images used…") naming exactly how many were left out, instead of silently dropping them.

**The smoking gun for how it happened:** `loom/master-storyboard.jsx:2451-2452` justifies its own label by citing `Gen.videoGenerate()`'s `runTask` call as live precedent — a function the same week's commit deleted, and which `tests/test_web_pick.py:1000` now asserts is absent.

**Not a drop, but the same release's biggest documentation casualty:** ~~the *published* GitHub wiki (what `README.md:19` calls "full documentation") is at `5d0b2e3`, dated 2026-07-17 — four releases stale. It still instructs new users to `pip install PySide6` and run `python pixai_gui.py`, says nothing about the login wall v2.0.0 added, and is missing The-Loom / Control-Panel / Trophy-Hall entirely (written today in `wiki/`, never pushed).~~ **FIXED 2026-07-22** (P1/D-10) — pushed to `a74b0d1`. D-10 (below) is still open: this was a manual push, and manual is exactly how it drifted the first time.

---

## 2. Decisions needed (answer in one line each)

| # | Question |
|---|---|
| D-1 | ~~On a failed free-card check at submit — hard-refuse, or submit and warn after?~~ **ANSWERED 2026-07-22**: refuse, but say it plainly and on-theme instead of a raw error. Shipped: "Lost to the Void — the free-card check didn't come back before submitting, so nothing was spent. Wait a moment and try again." |
| D-2 | ~~Should a LoRA/base architecture mismatch hard-refuse `/api/generate`, or warn-and-proceed?~~ **ANSWERED 2026-07-22**: let it through — PixAI's own site already rejects a mismatched pair and explains why, no need to duplicate that check server-side. Reverted the hard block (`81090c8`); the drawer's existing warning/disabled-Generate-button behavior for a mismatch is untouched. |
| D-3 | ~~Is the host-path disclosure on `/panel` intended, or should it be hidden off-loopback?~~ **ANSWERED 2026-07-22**: only the owner, at the actual PC, should ever see it. ~~Code fix queued — not yet shipped.~~ **Already FIXED 2026-07-21** — this row was stale bookkeeping, not a real gap. See S2 below (`pixai_gallery.py:8265`, `tests/test_route_tiers.py::test_panel_withholds_the_server_install_path_from_lan`), which landed the same night, a few commits after this row was written, and was never synced back here. |
| D-4 | ~~Partial-failure reporting: non-zero exit, or a third "finished with problems" status?~~ **FIXED 2026-07-22**: a new `done_with_errors` status, distinct from `done`/`failed`/`cancelled`. Exit code stays 0 by design (unchanged) — this is purely a louder, machine-readable signal: `run_download()` now returns its counts (it never did before — no caller could see `dl["fail"]`), the CLI logs `done_with_errors` via `_cli_job_finish(warn=N)`, the Panel subprocess gets a new `~=MGWARN=~N` stdout marker parsed by `_panel_reader`, the status is terminal/sticky/dismissable (`_JOBS_TERMINAL`, and the separate hardcoded tuple in `/api/jobs/dismiss`), and both the Panel's live status line and the Jobs tray render it distinctly (amber, not stuck looking like "running" forever). |
| D-5 | ~~`--variant` — delete, or wire it in?~~ **DELETED 2026-07-22**: verified directly (grepped the code myself, not taken on a prior audit's word) — the flag's value was read nowhere. Removed the argparse flag (confirmed: `"--variant"` no longer appears in `pixai_gallery_backup.py`). **Correction, re-verified 2026-07-23: the trailing claim is wrong.** `run_probe` (`pixai_gallery_backup.py:2848-2874`) calls `resolve_media()`, a separate mechanism, never `detect_variant()`. The whole `detect_variant`/`test_variant`/`media_url`/`MEDIA_TMPL`/`VARIANT_CANDIDATES` cluster (`:695-1350`) has zero callers anywhere in the repo, not even a test — same dead code O7 flags below. Only the flag itself was removed; O7 should stay open, not close on this row's word. |
| D-6 | ~~`--faststart-videos`: Panel button, or CLI-only?~~ **ANSWERED 2026-07-22**: CLI-only, deprecate-in-place — it's a rare one-time fix for old backups, new downloads don't need it. |
| D-7 | ~~Are prompt snippets and Loom storyboards personal or shared?~~ **FIXED 2026-07-22**: verified NEITHER was already per-account (a queued verification, not an assumption), then split both the same way saved views already were — per-account storage, read-only fallback to the legacy shared file/dir for an account with no copy of its own yet. See B14 below. One accepted, documented gap: an account can't force-delete a Loom board it inherited from the legacy layer but never wrote itself (matches the single-owner-plus-occasional-LAN-device case this was built for; revisit with a per-key tombstone if that ever stops being true). |
| D-8 | ~~`banner_reward`: build the unlock pool, or delete the "unlocks a banner" promises?~~ **REVISED 2026-07-22, then SCOPED 2026-07-23**: bigger than one banner. Owner's real design intent: a qualifying achievement unlocks a **bundle** — banner + icon/mark + a matching skin together — with reward *type* tracking achievement **tier** (low→icon, epic→skin, legendary→banner); Feats grew out of this same reward-tier design work, not a separate idea. Concrete mark decisions banked: Void Sentinel=default/free, Gem Tome=remove, Moonwell Eclipse↔Nightfallen skin, Vine Crescent↔Verdant Grove skin, Winged Crescent (owner remaking the art)↔Ember Court skin. Easter egg CONFIRMED as `under-the-hood`, but its "empty branding folder" trigger is rejected by the owner (the folder was never supposed to ship empty — his own art should ship as the real default) — trigger needs a redesign once defaults ship correctly. Banner-board picks are NOT recoverable from any doc — they live only in the voting browser's localStorage (see the artifact-ledger row); re-export or re-tag needed. Full detail in `docs/STATE.md`'s Open owner calls. Still needs: the tier→reward mapping finalized, banner defaults/unlock-pool split, and the easter-egg trigger redesigned, before this becomes buildable work. |
| D-9 | ~~Does the PWA actually install on `--host 0.0.0.0 --https`?~~ **ANSWERED 2026-07-22**: confirmed working, tested live on the owner's iPad Air (Loom reported to look great there too). Unblocked and closed the Cache Storage purge item (S4) that was waiting on this. |
| D-10 | ~~Automate the wiki push, or keep it manual?~~ **FIXED 2026-07-22**: `.github/workflows/wiki-sync.yml` pushes `wiki/*.md` to the published wiki on every release tag (`v*`), plus a manual `workflow_dispatch` trigger for a one-time smoke test. Needs one real run to prove `GITHUB_TOKEN`'s implicit wiki-push access actually works on this repo (high confidence from community precedent, not provable without trying) — fire it once via Actions → Sync wiki → Run workflow. If it fails with a permission error, the fix is a Personal Access Token as a repo secret (`WIKI_PUSH_TOKEN`), swapped in for `secrets.GITHUB_TOKEN` in the clone step. |
| D-11 | ~~Gallery adoption of `<mg-model-picker>`/`<mg-gallery-picker>`: parity, or demote to Loom-only?~~ **RESCOPED 2026-07-22**: not answered directly — surfaced two real, concrete problems instead. ~~The tooltip jump~~ **FIXED 2026-07-22**: both hover-preview copies (`pixai_gallery.py`'s `#model-flyout`, `static/mg-model-picker.js`) fired an instant, un-animated, freshly-repositioned popup on every card the mouse merely passed over — added a 130ms hover-intent debounce to both. ~~No LoRA option in the Loom's own picker~~ **FIXED 2026-07-22**: `static/mg-model-picker.js` gained an opt-in `multi` attribute (single-value mode untouched — zero regression risk to any future Gallery adoption); the Loom's Image tab gained a second `kind="lora" multi` mount, weight-editable chips, and Generate is disabled while any LoRA is still resolving or failed (mirrors the Gallery's own fail-open fix). `loraIncompat`/`resolveLoraPayload`/`anyLoraUnresolved` ported to `loom/src/loom-mutations.js` as pure, tested functions. Verified live against the real running app (not just source assertions): pending/resolved/failed/removed states all render correctly, Go-button gating works. Deliberately deferred: the base-model architecture-compatibility WARNING (would need the Loom to additionally resolve the selected base model's own type, which it doesn't today) — functionally safe to defer since PixAI's own server already rejects a real mismatch (D-2), but it means the Loom won't warn you before submit the way the Gallery's drawer does. The original parity-vs-demote question is still open underneath. |
| D-12 | ~~Close the 4-of-6 cost-badge gap now, or leave it parked?~~ **FIXED 2026-07-22**: all 4 of 4 shipped. The Gallery's Enhance sub-tab (`enhance-cost`) was also the one screen needing a real UX reshape (click-runs-immediately → select-then-run, mirroring the Edit sub-tab; its `window.confirm()` guardrail is gone, the badge is now the only warning, matching every other price surface). The Loom's Image/Edit/Reference Deep Focus tabs each gained an `<mg-cost-badge>` (`imgCostRef`/`editCostRef`/`refCostRef`), refreshed by a shared debounced `priceInto()` helper hitting the existing read-only `/api/price` — **deliberately kept alongside, not instead of**, each tab's existing `confirmSpend()`/`window.confirm()` gate at submit time: those three tabs' confirm dialog **is** the fail-closed guardrail that got built after they used to lie about cost (see `confirmSpend`'s own comment in `master-storyboard.jsx`), so the badge is an added preview, not a replacement — different call than the Gallery's Enhance tab, made deliberately. Also surfaced and explicitly logged as separate, out-of-scope items rather than silently dropped: the Fix sub-tab has no price check of any kind and `/v2/task-price` has no support for fixer params at all (a real backend gap, bigger than a badge swap); the "Generate all" batch/aggregate cost-to-finish pill and `<mg-cost-badge>`'s unused `compact` attribute are a natural follow-on, not part of this item. |
| D-14 | ~~Now that S1's five CLI paths are fixed, should `upload_media()` also honor `READ_ONLY`?~~ **ANSWERED 2026-07-22**: no, don't gate it. The apparent dispute was two different features being conflated — `/api/import-local` (the "Import into your library" dialog, genuinely local-only, never touches PixAI) vs. `/api/upload`/`upload_media()` (feeds a local file into an edit/reference request, does reach PixAI). Once separated, the owner confirmed: leave `upload_media()` un-gated. No code change needed — that's already its current behavior. |

---

## 3. The board

### Tier 1 — Security (5 items)

**S1. ~~`READ_ONLY` does not stop any CLI generation path.~~ FIXED 2026-07-21.** All five CLI
runners now call `_check_read_only()` as the first statement of their submit branch — before
`_apply_kaisuuken` and before any upload, not just before the mutation. `upload_media` was
deliberately left ungated (it costs no credits and isn't one of the four actions CLAUDE.md's
contract names); tracked as D-14 below if that should change. 10 new tests in
`tests/test_read_only_cli_paths.py`, 8 of which were verified to FAIL on the pre-fix source.
Original finding, for the record: *Verified.* `_check_read_only` is called at exactly four sites (`pixai_gallery_backup.py:1530, 4381, 4394, 5464`) against **seven** `gql_adhoc(session, _GEN_MUTATION, ...)` call sites. Five are unguarded CLI runners: `:4104` + `:4113` (`run_generate`), `:4506`, `:4599`, `:4677`, `:4768`. Two lenses proved it dynamically with the network stubbed — with `READ_ONLY=True` and `--confirm`, all five reach the mutation, and `_apply_kaisuuken` fires *first* (a live POST that can consume a free card). `wiki/Trust-and-Safety.md:64-67` promises the opposite "regardless of `--confirm`". `tests/test_read_only.py:6-8` asserts the false premise in prose and only tests the four choke-point functions, so the whole suite passes over it. **Fix:** route all five through `submit_generation()`, add CLI-entry-point tests asserting `session.post` is never called, correct the two docstrings. Also unguarded: `upload_media` (`:3938`), reachable under READ_ONLY from `--suggest-prompt`/`--enhance`/`--edit-image` with a local file — free, but the promise is "every account-mutating call". **45 min.**

**S2. ~~`/panel` hands account usernames and the host `out_dir` to any LOGIN caller~~
FIXED 2026-07-21 (the host-path half).** `panel()` now withholds the real `out_dir` from
a non-local caller the same way `/api/panel/status`'s `lines` field was redacted earlier
today — the route's LOGIN tier is unchanged, only that one field. Usernames are
**deliberately untouched**: account management being LOGIN-tier (any account may see and
manage any account) is a recorded design decision, not a gap, and this fix does not
revisit it. 4 new tests, verified to fail on the pre-fix source. Original finding: `pixai_gallery.py:8160` → `:7355` (`{{ u.username }}`), `:7345` (`library at {{ out_dir }}`). Add/remove enforce only the front door + CSRF; the only guard is refusing to remove the last account (`:8232-8236`), and removal signs the victim out everywhere (`:7435`). Enumeration is a **recorded decision** (`tests/test_route_tiers.py:73-77` carves account management out of the LOCALHOST rule explicitly; `wiki/Setup.md:69`; `pixai_gallery.py:8179-8182`) — so only the host-path half is unowned, and it's the exact thing `/api/panel/status` was redacted for today (`:8385`). Filed at `docs/STATE.md:617-621`, but that entry frames it as enumeration-only. **40 min.**

**S3. PARTIALLY FIXED 2026-07-21 — the unbounded-write half only.** Re-verified
`_watch_status["last_error"]` and `_export_job["error"]` against current source: both are
already `str(e)[:200]` at their write sites (fixed incidentally by other work tonight, or
the original citation was imprecise — either way, confirmed safe now). The one genuinely
uncapped site was real: `_cli_job_finish`'s `error=str(error)` had no limit at all. Fixed
at `append_job_event` — the single write choke point every job event from every source
(web routes, the Panel's subprocess reader, the CLI's own logging) funnels through — by
capping every string field at 200 chars there, matching this app's existing convention.
3 new tests, verified to fail on the pre-fix source.

~~**Still open, deliberately not attempted tonight: the broader 38-site `str(e)` redaction
sweep.**~~ **FIXED 2026-07-22.** A new `_redact_host_paths(msg)` closure in `create_app`
does literal-PREFIX `.replace()` (not regex — the exact thing that sank the first
attempt) against 5 candidates — `out_dir` (resolved, see below), `expanduser("~")`,
`gettempdir()`, `sys.prefix`, `getcwd()` — sorted longest-first so a nested candidate
(out_dir is very often somewhere under the home directory) redacts as one whole unit
rather than leaving a partial, still-identifying suffix. A small script mechanically
wrapped every real `str(e)` site — 37 of the 38, the 38th being a comment — with
`_redact_host_paths(str(e))`, applied before every existing `[:N]` truncation slice.
Both new tests are built around a **spaced** directory name specifically, re-testing the
exact regression class that killed the first attempt (`C:\Users\John Smith\...`), not a
convenient unspaced tmp dir. Adversarially reviewed (3 independent lenses) before
shipping; review caught and this pass fixed three more real gaps: (1) `out_dir` was
never `.resolve()`d before reaching the redaction candidates, so a relative `--out .`
turned it into a bare "." that matched — and mangled — every period in every error
message app-wide (reproduced, fixed by resolving + a length-floor guard); (2) two sites
in `api_setup_save_key()` build their error text with `.format(e)` rather than a bare
`str(e)`, invisible to a sweep that only matched the literal substring "str(e)" (fixed,
both localhost-gated so lower severity but still a real sweep-method gap); (3) one site
was missing its `[:N]` truncation cap (added, `[:200]`, matching its siblings).

Two residual gaps the review found but this pass did NOT fix, logged rather than
silently dropped: redaction only recognizes 5 known directory-path strings, not a bare
OS-username TOKEN sitting outside any of them (e.g. a username-prefixed filename with
no full path around it) — no live call site was found that would trigger this today,
flagged as defense-in-depth only. And `.replace()` is case-sensitive while Windows paths
are not, so a differently-cased path from a third-party library or normalized OS string
would silently fail to match — also no concrete trigger found in the current 37 sites.

~~**S4. Cache Storage survives sign-out.** `pixai_gallery.py:9001-9004` — the `/img/` and `/full/` branch is `r=>r||fetch(...)`, a cache hit never reaches the server, so the front door never sees it. The thumb branch (`:9010` `return r||n`) is the same. Nothing calls `caches.delete` outside the worker's own activate handler; `/logout` only clears the session. `wiki/Trust-and-Safety.md:27-28` ("nothing is browsable anonymously") and `:31-33` ("press Sign out if a session was captured") are both overstated. **The filed sequencing is wrong:** `docs/STATE.md:610` says the PWA question may moot this — it does not. `127.0.0.1` is a secure context, so the worker already registers and caches on the owner's own machine regardless of the LAN path. The doc fix is unconditional and cheap (**15 min**); the purge is genuinely blocked on D-9.~~ **FIXED 2026-07-22**: `logout()` now returns a real page (`_LOGOUT_HTML`) instead of a bare redirect — a 3xx can't run script, and the purge needs to. That page deletes every Cache Storage key client-side (`caches.keys()` → `caches.delete()` on each), then does the same client-side navigation to `/login` a redirect would have, with a visible `<a href="/login">` fallback link (added after adversarial review found the `<noscript>` fallback alone doesn't cover a browser that blocks just the inline `<script>` tag, not scripting entirely). Deliberately NOT hooked onto `/login` — verified in review that `login()` still has no signed-in short-circuit, so that would have reintroduced the exact flaw that sank the first draft. Fires unconditionally on any `/logout` hit (GET or a real POST), including an already-anonymous one; the CSRF-rejected 400 path is confirmed to never reach it, since that path leaves the session deliberately intact. `wiki/Trust-and-Safety.md` updated to describe the corrected behavior.

**S5. ~~Privacy Blur does not cross into any shared component, and the backend can't let it.~~ FULLY FIXED 2026-07-23.** `body.privacy-blur` originally had three rules, all keyed on `.card img` (`pixai_gallery.py:3593-3595`) — none of the gallery Picker (`pick-cell`), `<mg-gallery-picker>` (`.mg-pk-cell`), the Generate drawer's reference slots (`mg-generate-drawer.js`'s `slotBox()` boxes, now classed `.mgd-slot`), or the Edit tab's single reference slot (`#gen-ref-slot`, found during this pass — same gap, not in the original citation) rendered `.card`, so Privacy Blur never touched any of them regardless of the NSFW flag. `/api/gallery-images` now projects `is_nsfw` alongside `/api/similar`. `is_nsfw` is threaded end to end through the pick-event chain on BOTH host paths — the main gallery page's `Picker.open`/`pick()`/`mg-pick-request` listener, and the Loom's `openPick`/`bindGalleryPicker`'s `mg-pick` listener — since the drawer's ref-slot thumbnail only ever learns whether its pick was NSFW via that event chain, not from `/api/gallery-images` directly. Each of the four surfaces got its own `data-nsfw` setter plus a `body.privacy-blur <selector> img` rule pair (16px base blur + 28px NSFW blur + hover-clear), mirroring `.card`'s exact shape. Fail-first tests: `tests/test_web_pick.py::test_gallery_images_includes_is_nsfw_for_privacy_blur` (server) and `::test_privacy_blur_covers_the_picker_and_drawer_reference_surfaces` (all four client surfaces + the Loom bridge + the rebuilt esbuild bundle), both verified to fail against the pre-fix source. Full suite green (869 Python, 128 Loom). Additionally verified live: every CSS rule was checked against a real Chromium computed-style pass (`getComputedStyle(img).filter`) for all four surfaces in both the plain and `data-nsfw="1"` state — not just asserted as a string in source.

> **Security tier realistic clear time: ~4.5 focused hours** (S1 45m + S2 40m + S3 135m + S4 15m doc + S5 45m — S5 revised 2026-07-23, its Similar-modal half shipped), i.e. a solid half-day. S4 (doc + cache purge) shipped 2026-07-22. **All five Tier-1 security items are now closed.**

---

### Tier 2 — User-visible breakage & silent data loss (18 items)

**B1. ~~Zero-byte files are permanently unrecoverable and permanently served.~~ FIXED
2026-07-21.** All three sites now agree: the resume index skips a 0-byte on-disk file
(re-downloads it instead of treating it as done), `download()` refuses to promote an
empty response body to a real filename, and `find_image_file`'s fast path now matches its
own fallback's size check. 6 new tests across `tests/test_network.py` and
`tests/test_dedup.py`, all verified to FAIL on the pre-fix source. Original finding: `pixai_gallery_backup.py:6001-6010` — the O(1) resume index filters `.part` and extension but never size, and `e.stat()` is already taken two lines down for `disk_bytes`. So a 0-byte image is indexed as present → skipped by every `--update`/`--sync` forever → `reconcile_catalog_with_disk` finds nothing via the strict matcher and leaves the row intact. `download()` (`:1451-1456`) can *create* the file with no `nbytes == 0` guard. And `find_image_file` (`pixai_gallery.py:2284-2297`) tries the catalog `filename` first with `is_file()` but **no size check**, so the gallery serves it back. `already_downloaded()` — the function `tests/test_filesystem.py:40` guards — correctly reports it missing; run_download does not call it. **Two lines. ~25 min.**

**B2. ~~`--dedup --dedup-delete` can hard-delete the real image and keep the empty one.~~
FIXED 2026-07-21.** A zero-byte file is now excluded from the audit's duplicate pool at
the source (`audit_collection`), so it can never be selected as a keeper OR a loser —
plus a belt-and-braces size check in `_keeper()` itself. 3 new tests in
`tests/test_dedup.py`, all verified to FAIL on the pre-fix source, including one that
drives the real `--dedup --apply --dedup-delete` CLI action end to end. Original finding: `_scan_media_files` (`:2110-2116`) filters `.part` but not size; keeper selection is bucket-priority + path length only (`:2088`, `:2157`), never bytes. A 0-byte `2024-03/x.webp` (prio 1) beats a real `images/x.webp` (prio 2). The auto-verify safety net at `:2339` is explicitly skipped in delete mode (`if not delete and moved:`). This is **a Control Panel button**, not a CLI incantation — `pixai_gallery.py:2952-2955` `"dedup-delete"`, labelled "DELETE dupes outright (no `_duplicates/` safety net)". **~30 min.**

**B3. ~~CLI batch generations silently keep only the composite grid.~~ FIXED 2026-07-22** (commit `fcfec8b`, "Fix batch>1 --generate/--edit-image saving only the composite grid"). Both `run_generate` (`:4253`) and `run_edit_image` (`:4908`) now build their media list via `_task_image_media(outputs)` instead of reading `outputs.batchMediaIds` directly; `run_generate`'s video-collection loop (now `:4256-4258`) was kept alongside the swap, so the video-recovery caution never materialized. New tests: `tests/test_model_grid.py::test_run_generate_saves_batch_individuals_not_the_grid`, `::test_run_edit_image_saves_batch_individuals_not_the_grid`. This board row was missed even though the appendix's `unfiled-workflow-findings` duplicate already carries the same FIXED note. Original finding, for the record: `outputs.batchMediaIds` is null on modern tasks; the web/Loom paths already used `_task_image_media` and these two CLI runners were left behind by `d616e76`.

**B4. ~~Gallery Video tab: no `mg-submit`/`mg-result` listener.~~ FIXED 2026-07-21.** See §1.

~~**B5. A transiently-failed free-card check silently spends after the badge said "0 credits".** `match_kaisuuken` (`pixai_gallery_backup.py:5329-5332`) collapses `PixAIError`/`ValueError` into `None`, indistinguishable from "no card matched"; `_apply_kaisuuken` (`:5550-5558`) prints "this will spend credits" to the *server console* and returns; `/api/generate` returns `{task_id}` with nothing about coverage. The price check and the submit check are separate network calls seconds apart. **The fix must cover four call sites, not three** — `/api/generate` `:10475`, `/api/edit` `:10506`, `/api/enhance` `:10536`, and **`/api/loom/generate` `:10861`**, the video path, the ~210k-credit one. Note `tests/test_kaisuuken.py:137` pins the fails-soft contract, so the strict mode belongs in `_apply_kaisuuken` or a new kwarg, not in `match_kaisuuken`'s default. **~45 min.**~~ **FIXED 2026-07-22** (duplicate of the appendix `fail-open` row, struck there but missed here): `match_kaisuuken(raise_on_error=True)` + `_apply_kaisuuken` (`pixai_gallery_backup.py:5439-5717`) now retry the check once, then abort with a `PixAIError` ("Lost to the Void…") instead of falling through to "no card -> pay credits." Verified all four web call sites (`/api/generate` `pixai_gallery.py:10963`, `/api/edit` `:10999`, `/api/enhance` `:11024`, `/api/loom/generate` `:11409`) plus 5 CLI submit paths (`pixai_gallery_backup.py:4218,4622,4719,4799,4893`) all funnel through the shared `_apply_kaisuuken` choke point — nine call sites total, none bypassing it. `tests/test_kaisuuken.py::test_apply_kaisuuken_check_failure_aborts_instead_of_silently_paying` (line 187) covers the abort path; the fails-soft `test_match_fails_soft` (line 137) still passes since only `_apply_kaisuuken` opts into strict mode.

**B6. ~~A LoRA whose version lookup fails is silently dropped from a paid generation.~~ FIXED 2026-07-22** (see the `fail-open` appendix duplicate below, which already carries this fix — this board row was missed). `toggleLora()`'s fetch (`pixai_gallery.py:6112-6128`) now marks `entry.failed` on both an empty successful response and a network `.catch`; `updateGoState()` (`:6151`) gates on a new `anyLoraUnresolved()` (`:6150`) alongside the base model; and `generate()` (`:6324-6331`) refuses to submit while any LoRA is unresolved even if the Go button's disabled state is bypassed — verified all three directly in the current source.

**B7. `is_lora_compatible()` — the architecture-mismatch spend gate — has zero production callers.** ~~*Verified:* definition (`:1880`), one prose mention (`:1844`), tests only. The rule is re-implemented in browser JS (`pixai_gallery.py:5951`) whose only effect is `go.disabled`. `_gen_args_from_payload` copies version ids straight from the client (`:10139-10142`); the CLI's `--lora` path has no gate (`:3523`). By the helper's own docstring, the consequence is a server-side rejection *after* the spend.~~ **ANSWERED 2026-07-22 (D-2)**: deliberately left unwired. A server-side hard block was built, then reverted (`81090c8`) on the owner's call — PixAI's own site already rejects a mismatched pair and explains why; the pre-existing client-side `go.disabled` hint stays as-is. Zero production callers is now the intended, decided state, not a gap.

**B8. ~~`clamp_edit_config()` never runs on the CLI edit path.~~ FIXED 2026-07-22** (Package 3; see the two appendix duplicates below, both already struck while this board row was missed). `_edit_config_from_args` (`pixai_gallery_backup.py:4085-4096`) now calls `clamp_edit_config()` itself before returning, covering both the CLI preview and the real submit — confirmed directly in the current source.

**B9. ~~`_snap_video_duration()` never runs on either CLI video path.~~ FIXED 2026-07-23** (commit `d812c75`): both `_gen_video_parameters` (i2v) and `run_reference_video`'s internal `_build` now snap before preview/submit, matching `build_shot_video_params`. Fail-first tests in `tests/test_video_gen.py`, adversarially reviewed (independently reverted, both failed for the claimed reason, restored).

**B10. ~~`--reference-video`'s duration default is 5 or 15 depending on which line you read.~~ FIXED 2026-07-23** (commit `d812c75`): unified to 5 everywhere (matching the i2v sibling and the real argparse default). Regression test locks all three places against re-drift.

**B11. ~~`_deleted/` is invisible to only one of the bulk walks.~~ FIXED 2026-07-23** (commits `a21c20f`/`8d5b7a9`/`678d9aa`): all five gap sites (resume, the audit's shared scanner, `--import-local`, `duplicate_groups`, `cmd_organize`) now exclude `DELETED_DIRNAME`. `cmd_organize`'s bug was reproduced live before fixing — it really did silently `unlink()` a quarantined file sharing a media_id with the live copy. 5 regression tests, one per walk, each independently confirmed to fail pre-fix by an adversarial reviewer who reverted just the source and reran them.

**B12. ~~`/export-zip` discards every convert/embed status note.~~ FIXED 2026-07-23** (commit `4d07548`): `export_zip()` (`pixai_gallery.py:9382-9479`) now collects every `convert_image()`/`embed_metadata()` status note and any per-file transform exception into a `warnings` list; when non-empty it's written as `_export_warnings.txt` inside the zip plus an `X-Export-Warnings` response header (count). Covers all three reachable holes named below: Pillow-absent, `fmt=original`+embed-on-`.webp` (silent `unsupported`), and a per-file transform exception. Videos are never transformed (`if tmp and not row.get('is_video')`), so mp4 pass-through stays untouched, matching `tests/test_gallery_filters.py:432-446`. ~~`pixai_gallery.py:9078-9089` throws away both return values, and the `except Exception` can't catch the likely failures because neither helper raises — `convert_image` returns `("pillow-missing")`, `embed_metadata` returns `"unsupported"` for `.webp`. **Corrected scope:** conversion runs *before* embedding, so webp→jpg+embed works. The real reachable holes are (a) Pillow absent — and every export test does `pytest.importorskip("PIL")`, so that path is untested; (b) `fmt=original` + embed on a `.webp` → silent `unsupported`; (c) a per-file convert error. Any `EXPORT_NOTES.txt` must exclude videos — `tests/test_gallery_filters.py:432-446` pins mp4 pass-through as deliberate. **~30 min.**~~

**B13. ~~A transient blip on `/api/task-status` is reported as a terminal failure and the poller gives up.~~ FIXED 2026-07-23** (commit `4d07548`): the generic `except Exception` branch of `api_task_status()` (`pixai_gallery.py:11752-11768`) now returns `{"phase": "running", "status": "checking… (...)"}` instead of `{"phase": "failed"}` — `static/mg-notify.js`'s poller treats anything but `done`/`failed` as non-terminal and retries. The `EmptyOutputsError` branch (`:11743-11751`) is unchanged and stays terminal, as this row itself required.

**B14. ~~Per-account saved views are defeated on Windows by filename case.~~ FULLY FIXED 2026-07-23** (commit `4f32396`, on top of D-7's 2026-07-22 per-account split). One shared `_account_key(username)` (sha256-derived, case-collision-safe) now backs all four per-account stores — saved views, prompt snippets, Loom storyboards, toolbox presets — replacing each store's own case-preserving `quote(username)`. Reproduced the real collision end-to-end on this machine for all four stores before fixing, confirmed independent storage after. **Owner-relevant note:** any per-account file created under the ~13.5-hour-old D-7 `quote()` scheme (i.e. if this exact machine ran the app between D-7 and this fix) won't be found at its new hash-derived path — nothing is deleted, it just falls through to the older pre-D-7 shared file. No migration path was added (out of scope, narrow window); check `out_dir/{view_presets,prompt_snippets,toolbox_presets,loom/kv}/` by hand if that window matters to you.

**B15. ~~A backup that failed N downloads reports "✓ finished (exit 0)".~~ FIXED 2026-07-23** (commit `6416d2b`). `--sync`'s own half was found already fixed as a side effect of the D-4 commit itself (verified via `git show 044dd6b` — the citation had drifted, no code change needed there). `run_sync_artworks` was the real remaining gap: it now tracks incomplete-pagination + failed-video-download counts and reports through the exact same `done_with_errors`/`~=MGWARN=~N` mechanism D-4 already built for `run_download`, rather than a second parallel mechanism.

**B16. ~~`already_downloaded()` is a guaranteed-False no-op for videos.~~ FIXED 2026-07-23** (commit `8d5b7a9`): `find_files_for_media_id` gained an optional `exts` param (default unchanged, every 2-arg caller untouched — verified `tests/test_loom_export_bundle.py:87` covers a genuinely different code path and stayed green), and a new `already_downloaded_video()` sibling uses it with `_VIDEO_EXTS`. `run_sync_artworks`'s `--with-videos` resume check now calls the video-aware sibling.

**B17. ~~The Loom's last-frame resolver uses a bare `*<mid>.*` glob.~~ FIXED 2026-07-23** (commit `678d9aa`): the route is actually `/api/loom/handoff` (`loom_handoff`), not `/api/loom/last-frame` as originally cited — its fallback now calls the same generalized `find_files_for_media_id(exts=vid_exts)` B16 built, giving it exact-match + quarantine exclusion for free.

**B18. ~~Silent-failure cluster (five small ones).~~ ALL FIVE FIXED 2026-07-23.** ~~Prompt-snippet saves are fire-and-forget against an endpoint that returns **200 with an error body** on write failure (`:5756` / `:9751-9754`) — deletes reappear on reload.~~ **FIXED**: `Snips.persist()` (`pixai_gallery.py:5919-5930`) now checks the response and shows a `Toast` error ("Snippet not saved") on a server-side write failure. ~~`/api/loom/delete` returns `ok:true` on `OSError` and on a missing key (`:10750-10760`), while its sibling `/api/loom/set` does it right.~~ **FIXED**: `loom_delete()` (`:11280-11307`) now returns `{"ok": false, "error": ...}` (500) on a real `OSError`, distinct from `FileNotFoundError` (still `{"ok": true}` by design — deleting an already-gone key stays a success), matching `loom_set`. ~~`/api/skin` and `/api/achievements?mark=1` return 200 when `save_ach_state` returned `False` (`:9864`, `:9912`) — skin snaps back on reload, unlock toast repeats every page load.~~ **FIXED**: both routes (`:10298-10299` achievements, `:10349-10356` skin) now check `save_ach_state()`'s real return value; on a write failure they add an `error` field and report the actually-active value (a fresh re-read) instead of silently claiming success. ~~`savePreset()` shows nothing on a 400 "name required".~~ **FIXED**: `savePreset()` (`:4479-4489`) now shows a `Toast` error when the server rejects a save. ~~`--suggest-prompt` has no video gate although the web template has one (`:6938-6945`) — raw 500 at the user.~~ **FIXED 2026-07-23** (commit `d812c75`): `run_suggest_prompt` now refuses a video media_id/file before touching the network, mirroring the web gate. All five of five now fixed.

---

### Tier 3 — Orphaned & half-migrated (14 items)

**O1. `ENHANCE_PLUGINS` is fully unreachable.** ~~*Verified:* defined at `pixai_gallery.py:2565`, read only at `:10526` in the `elif plug:` branch, and a repo-wide grep for a `plugin:` sender across `static/*.js`, `pixai_gallery.py` and the Loom returns **nothing**. All three entries are dead — including `detail-fix`, the one the comment calls "the VERIFIED workflowId (fired for real 2026-07-02)", which is **not** among the ten hardcoded ids in the Enhance shelf (`:5231-5243`). `tests/test_enhance.py:23` is green over it. Two roadmap notes are wrong about this: the "web Fix tab rides this path" claim is false (`/api/fix` → `submit_fixer` → `/task/fixer`, a different transport), and the "curated cards shipped" claim is false — the shelf bypasses the dict entirely. **Recommendation: delete** the dict, the `elif plug:` branch, the `workflow_name` kwarg and its test. ~30 min.~~ **DELETED 2026-07-22** — this narrative entry was missed when the appendix row for the same finding got marked FIXED hours earlier the same night (commit `35a8509`); same doc-drift pattern this whole file exists to prevent, caught while working D-12.

**O2. ~~`frameLinked()` — zero call sites.~~ REBUILT 2026-07-23** (commit `b8de324`, owner's explicit call: "Rebuild it," not delete). Lives in `loom/src/loom-core.js` (citation drift — not `loom-mutations.js`). New `continuityLinked(entries, entryId)` gives it its first real caller, wired into a small cyan "linked" badge on `LoomV2`'s board card (next to the mode/duration tags) that renders only when a shot's opening frame already matches the previous shot's closing frame. Confirmed genuinely distinct from the existing Mode/Continuity "First→Last" chip (that couples a shot's own generation mode to itself; this reads actual frame identity across a cut). **⚠ Needs your visual sign-off** — this is the one real UI judgment call in the whole sweep, not a pure bugfix. Two open questions: (1) silent/positive-only badge vs. also flagging non-linked shots (bigger call, deliberately not made — would need to account for intentional scene breaks); (2) board card vs. Deep Focus as the badge's home (board card chosen for glanceability). 13 new tests (8 pure-logic + 5 source-presence), adversarially reviewed including an independent byte-diff of the rebuilt esbuild bundle.

**O3. ~~`data-loom-ctx` has zero setters, so the Loom's Video panel renders two Camera controls and two quality controls.~~ FIXED 2026-07-23** (commit `2b891df`). The `<mg-generate-drawer>` mount in `loom/master-storyboard.jsx:1419` now sets `data-loom-ctx=""`, activating the CSS hide rule already sitting in `static/mg-generate-drawer.js:130` (`mg-generate-drawer[data-loom-ctx] .mgd-cam-wrap,mg-generate-drawer[data-loom-ctx] .mgd-quality-wrap{display:none;}`) — the drawer's own Camera select and Basic/Professional select are now hidden in the Loom, leaving only the Loom's free-text Camera field and top-strip Draft toggle. New regression test `loom/test/loom-gen-drawer-loom-ctx.test.js` source-checks the JSX mount for the attribute.

**O4. ~~`/api/view-presets` accepts `POST {delete: name}` with no UI.~~ FIXED 2026-07-23**: added the missing delete-view control — a control next to the "Saved views…" select, wired to a new `deletePreset()` (`pixai_gallery.py:4490-4505`) that confirms, then POSTs the pre-existing `{delete: name}` shape to `/api/view-presets`. ~~`pixai_gallery.py:10356-10357`; the route's own docstring admits it (`:10335`) and cites "the select's reserved delete affordance" — which does not exist. Saved views can be created and never removed except by hand-editing `out_dir/view_presets/<account>.json`. Per-account views shipped **today**, so the sets are fresh and will accumulate. **~30 min.**~~ Residual, not touched in this pass: the route's own docstring (now `pixai_gallery.py:10816-10817`) still reads "no UI for it yet -- the verb exists so the select's reserved delete affordance has something to call" — now stale, since the affordance exists.

**O5. ~~`/contact-sheet?collection=` is fully implemented with no UI entry.~~ FIXED 2026-07-23** (commit `e38c784`): added a "Contact sheet" button beside the existing "Download collection" ZIP button, same `{% if collection %}` gating, same `data-coll` plumbing.

**O6. ~~Three GUI-only wrappers with zero callers.~~ FIXED 2026-07-23** (commit `a4a3de7`, "Delete unreachable GUI-era wrappers"). `run_verify_dupes`, `run_audit`, and `run_dedup` are all gone from `pixai_gallery_backup.py` — verified via repo-wide grep, zero remaining definitions or references. The real `cmd_verify_dupes` (`:2497`)/`cmd_audit` (`:2298`)/`cmd_dedup` (`:2340`) logic they wrapped is untouched and still dispatched directly from `main()`. The same commit also deleted `is_lora_type()` (see O8 below).

**O7. ~~`--variant` is the only dead flag in the CLI.~~ CLUSTER DELETED 2026-07-23** (commit `c587153`): `detect_variant`, `test_variant`, `media_url`, `MEDIA_TMPL`, `VARIANT_CANDIDATES` all removed, plus the module docstring's stale auto-detect claim corrected. Live siblings `MEDIA_BASE`/`resolve_media`/`URL_VARIANT_PREFERENCE` confirmed untouched.

**O8. ~~`is_lora_type()` (`:1706`) — defined, referenced nowhere, not even by a test.~~ FIXED 2026-07-23**: deleted in the same commit as O6 above (`a4a3de7`); confirmed zero remaining references anywhere in the repo (grepped py/js/jsx). The two inline call sites it duplicated (formerly `:1684`, `:1791`) are untouched.

**O9. ~~`.vp-chip` / `.gen-ce` — 5 dead CSS rules.~~ FIXED 2026-07-23** (commit `4d07548`) — see §1: both deleted; `pixai_gallery.py` now has zero matches for either selector. **Broader sweep now done 2026-07-23** (commit `893a8e2`): all ~290 class selectors across `pixai_gallery.py`'s inline `<style>` blocks mechanically extracted and cross-checked for a real producer; 22 automation-flagged candidates were manually verified as false positives (runtime string-concat/ternary class-building), 2 were genuinely dead (`#gen-drawer.dock-right` — unreachable, since the default/no-class state already reads as "right"; `#model-preview .mp-tags` — never populated) and removed.

**O10. The stray root `HEAD` file — mystery solved, and the `.gitignore` comment is a false diagnosis.** ~~`pixai_gallery.py:2782-2787` shells `git rev-parse --short HEAD`, so `argv[-1]` is the literal `"HEAD"`. `tests/test_panel.py:471-473` and `:554-557` monkeypatch `subprocess.Popen` **globally** with `FakeProc.__init__` doing `open(argv[-1],"wb").write(b"OUTPUT")`, and `create_app()` runs after the patch. Reproduced twice by two lenses. It has now been deleted and re-diagnosed as a mystery at least twice. **Fix:** `if argv and argv[0] == "ffmpeg":` in both FakeProcs. Then correct `.gitignore:52-53`, which claims it was "born from a `> HEAD` shell redirect" — that comment is committed and is what will let the next reader dismiss it a fourth time. **~10 min.**~~ **FIXED 2026-07-23** (commit `1ba9505`): both `FakeProc` classes in `tests/test_panel.py` that write to `argv[-1]` (now at lines ~510 and ~599) guard on `argv[0] == "ffmpeg"` before writing, with comments explicitly citing this finding. `pixai_gallery.py`'s `_build_stamp()` (now `:2820-2836`, shells `["git","rev-parse","--short","HEAD"]` through that same globally-patched `Popen`) no longer hits the write. `.gitignore`'s `/HEAD` entry comment (now `:71-79`) is corrected from "born from a `> HEAD` shell redirect" to the real cause. No stray root `HEAD` file currently exists in the repo.

**O11. ~~`generateShot` is a dead prop in `LoomV2`.~~ FIXED 2026-07-23** (commit `e38c784`): removed from both `LoomV2`'s destructured signature and the `<LoomV2 .../>` call site; the real implementation in `useGenerationPipeline` (still called internally by the batch-retry path) is untouched. **Residual, out of this item's stated scope:** `App()`'s own separate destructuring of `generateShot` from the hook's return value is now an unused local binding too (harmless — no lint step enforces this) — flagged for a stricter follow-up pass if wanted, not fixed here.

**O12. `mg-model-picker` can't replace `#model-flyout`.** It sends `size=12` with no LoRA half at all (multi-select, weights, compat gate, trigger words all live only in the inline `Gen` IIFE, `:5924-5988`). *Corrected from the original finding:* sort/category are **not** a real gap — the gallery hides them for base models by design (`:5818-5821`) and the component mounts base-only; neither picker paginates. So the honest residue is `size=12` vs 24 and the whole LoRA surface.

**O13. The picker drift now runs both ways.** `<mg-gallery-picker>` has a persisted 90–240px Size slider (`:195`, added on the owner's request) that the gallery's own `#pick-modal` never got (fixed 120px grid, `pixai_gallery.py:5053`). Neither implementation is a superset, which is what keeps this migration step frozen.

**O14. `<mg-cost-badge>`'s `compact` attribute and `mg-cost` event have no production consumer.** Both are declared public API of a deliberately host-agnostic component, so this is not an orphan in the defect sense — but it matters for D-12: `compact` was built for the cost-to-finish pill and `mg-cost` is precisely the DOM-level signal that crosses the no-build-global ↔ esbuild-module wall the consolidation stopped at. **Note it in STATE so the next session doesn't conclude the wall is uncrossable.**

*Also: ~~`ARTWORK_DETAIL_HASH` is read from config with a baked default and used nowhere~~ **FIXED 2026-07-23** (commit `c587153`): dead read + its `config.example.json` mention both removed; live sibling `ARTWORK_LIST_HASH` untouched. `catalog_model_options()` is kept alive only by its own test (deliberate, recorded in `ea24497`); ~~`--open-browser`'s help text credits a launcher that stopped passing it in `7c94f9e`~~ **FIXED 2026-07-23**: reworded to "manual convenience for a terminal launch; the double-click 'Serve Gallery' launcher does NOT pass this" (`pixai_gallery.py:11935-11939`); ~~`picker-z-inventory` is a fully-merged zero-commit branch sitting on origin~~ **deleted 2026-07-23**, along with the 7 temporary `audit-sweep-*` branches this pass used and merged.*

---

### Tier 4 — Doc lies (grouped into 7 passes, ~35 individual claims)

**P1 — Push the wiki (D-10).** ~~The published wiki is 4 releases stale; 10 of 13 shared pages differ; 3 pages were never pushed. It tells strangers to run the deleted GUI.~~ **FIXED 2026-07-22**: cloned `moonglade-athenaeum.wiki.git`, replaced every page with the current fact-checked `wiki/` source (10 changed, 3 added — The-Loom, Control-Panel, Folio-of-Honors), pushed (`5d0b2e3` → `a74b0d1`). Confirmed with the owner in chat before publishing, since this touches a public surface outside the main repo push this session was otherwise authorized for.

~~**P2 — The localhost/LAN model, told three incompatible ways.**~~ `wiki/Generating.md`/`Backing-Up.md`/`Control-Panel.md` **FIXED 2026-07-21.** `docs/architecture.md:212`'s `/api/branding` claim and the four route docstrings (`loom_generate`, `api_model_search`, `api_ping`, `api_branding`) **FIXED 2026-07-22** — all corrected to LOGIN, matching `tests/test_route_tiers.py`'s `ROUTE_TIERS`.

**P3 — STATE.md's self-contradictions.** ~~`:350` "exactly four things" vs `:361` "PUBLIC (the three above)"~~ **checked 2026-07-22, already correct** (both say "four", matching `PUBLIC_EXPECTED_STATUS`'s real 4 endpoints) — probably fixed by the same earlier pass as the "five vs seven LOCALHOST routes" line below. ~~"Only five routes are LOCALHOST"...~~ **FIXED 2026-07-22.** ~~`:446` "What's still CLI-only … Nothing"~~, ~~`:422-424` "the only write site for `c.prompt`"~~, ~~`:488-490`'s Design Pass "previously filed individually" self-contradiction~~, ~~`:782` implying the banner-unlock reward is live~~ — **all FIXED 2026-07-22.** ~~`:434-438` "`<mg-cost-badge>` covers four of six … the picker path"~~ — **already correct 2026-07-22**, rewritten during tonight's D-12 rollout before this pass ever reached it. ~~The original `:623` citation for the `is_local=True` reasoning gap was itself stale — the underlying gap is real (doesn't account for the "↑ Import" button).~~ **Comment fixed 2026-07-23** (commit `e36976d`): both nearby comments (the head-nav block and the render_template_string call site) now explicitly name Import as the documented exception — `/api/import-local` itself is correctly LOCALHOST-gated, so there was never a real security hole, just a misleading comment. A deeper fix (gating the button's visibility on the real local check instead of the blanket flag) remains a UX/code call left to the owner, not made here.

~~**P4 — CHANGELOG v2.2.0.** "Every cost display in the app is now the same component … and the picker" — false in four places and inflates the denominator. `:130` gives the saved-views path as `out_dir/view_presets.json`, which **this release replaced**. `:11-14`'s maintenance note says Releases are published through v1.10.0 (they're published through v2.2.0) and that v1.11.0 "never reached master" (`git merge-base --is-ancestor` says it did) — and CLAUDE.md explicitly says publication status is a live fact never to be transcribed.~~ **FIXED 2026-07-22**: "and the picker" dropped (no picker anywhere renders a cost badge — confirmed by grep, and by re-reading the shipping commit's own scope); saved-views path updated to `out_dir/view_presets/<account>.json` (confirmed the per-account split had already landed before v2.2.0 was actually tagged, via `git merge-base`/commit timestamps); maintenance note's version and v1.11.0 claims both corrected against `gh release list`/`git merge-base --is-ancestor`.

~~**P5 — Two shipped UI strings point at the deleted GUI.** `pixai_gallery.py:6689` ("paste into the GUI Video/Edit tab") sits four lines from an "Edit this" button that deep-links the real web tab. `:7145` is worse — it's the **only** next-step instruction on `/duplicates` ("run Dedup from the GUI Utilities tab"), and that page is only reachable when you *do* have duplicates and *do* need the next step. The real destination is Panel → `dedup-apply` (`:2948`). While there: `:7123-7124` on the Health page has the same shape one screen earlier, pointing at a terminal. Also ~12 comments/docstrings still describe the GUI as a live surface, two of which (`_make_session`, `run_download`'s progress callback) explain *why* code is shaped the way it is by pointing at a module that's gone.~~ **FIXED 2026-07-22, count corrected**: verification found the Health-page claim was already stale (it points at the still-live `--dedup` CLI flag, not the GUI — left alone), but caught a real second string the original search hints mis-located (the detail page's "Copy media id" tooltip). Both real strings + the `/duplicates` page now point at the real web destinations. A case-sensitive `\bGUI\b` sweep of the whole file (not just the ~12 estimated) turned up 3 more comments, all fixed; two of them (the Panel subprocess comment and the browser-open timer comment — not `_make_session`/`run_download`'s progress callback, which don't exist in this file) had their WHY-shaped-this-way reasoning preserved rather than deleted outright. `\bGUI\b` now returns zero matches in `pixai_gallery.py`.

~~**P6 — CLI help/errors that manufacture setup steps.** `--full-meta`'s help demands `TASK_DETAIL_HASH + MODEL_DETAIL_HASH in config.json` when both ship baked in (`:657-662`, and `_cfg.get(...) or <default>` can never be falsy); three guards cite a README "Full Meta" section that does not exist. `--delete-task` help says "Requires DELETE_TASK_HASH" — the delete path's own comment already got this right ("Defensive only … NOT a setup gate").~~ **FIXED 2026-07-22**: all four guard messages + both help strings rewritten; guards themselves kept in place (`tests/test_network.py`'s pin untouched). **The real bug hiding under this is confirmed still open, NOT a docs fix** — `_task_detail_query`'s docstring claim re-traced start to finish: its only real caller is `collect_generation` (task-id recovery), while `run_backfill_full_meta` and `run_download`'s `--full-meta` branch both call `task_detail_gql` directly, bypassing the fallback entirely. **Docstring corrected 2026-07-23** (commit `e36976d`) to accurately name the split, intentionally without rewiring the two callers — that's still a real behavior change needing an owner call, not made here.

**P7 — Reference docs vs code.** ~~`docs/architecture.md` says thumbnails are served `immutable` for a year; the code says `max-age=300` with an eight-line comment explaining that `--rebuild-thumbs` rewrites in place (`pixai_gallery.py:8903-8908`) — the doc pass ran 2026-07-16, the fix landed 07-17, nobody came back. `already_downloaded()` is described with the wrong glob (`*_<mediaId>.*` — the missing underscore *is* the fix, regression-tested) and called "the resume check", which it isn't.~~ **FIXED 2026-07-23** (commit `d017198`): the layout diagram + thumbnail-cache prose now read `public, max-age=300 (short, not immutable)` (`docs/architecture.md:161`, code confirmed at `pixai_gallery.py:9258`); the `already_downloaded()` module-table row (`docs/architecture.md:33`) now describes the real `find_files_for_media_id` matcher and states plainly that `run_download` does NOT call it (only `run_sync_artworks`'s video resume does). ~~**INVARIANT 7** ("one shared media-id→file matcher; resume, gallery and audit all share it") is false — there are 11+ independent walks with four exclusion sets. **INVARIANT 2** ("checked before any network call") is false under `--full-meta`/`--sync`. **INVARIANT 4** ("catalog is the source of truth for organize and friends") is contradicted by dedup's own section header. **INVARIANT 1**'s "single source" `media_id_of()` is re-implemented inline twice.~~ **All four reconciled 2026-07-23** (commit `e36976d`): Invariant 7 now names `cmd_organize`, `run_import_local`, `duplicate_groups`, and `/api/loom/handoff`'s fallback (`loom_handoff`) as the independent walkers (B11, merged separately the same sweep, subsequently made each respect the same quarantine exclusion — without consolidating them into one shared call, so this doc claim stays accurate). Invariant 2 now states the `--full-meta`/`--sync` exception explicitly. Invariant 4 reconciled with `audit_collection()`'s own "Filesystem-truth" description. Invariant 1's module-table row no longer claims "single source" — re-verified only ONE other module truly duplicates it (`pixai_similar.py`'s `scan_dir`, not "at least two" as originally cited), corrected accordingly. `CONTRIBUTING.md`'s matching false claim fixed too, repointed to `docs/architecture.md` as the real source. ~~And `docs/architecture.md` **never mentions `static/mg-*.js` or the five shared components at all**, while its Video-tab bullet still describes the pre-swap hand-rolled form.~~ **FIXED 2026-07-23** (commit `d017198`), the missing-section half: a new "Shared web components (`static/`)" section (`docs/architecture.md:234-254`) now documents all 5 `mg-*.js` custom elements plus `picker-core.js`. The Video-tab-bullet half was independently re-checked and found **not stale**: "The web suite" section's Generate-drawer bullet (`docs/architecture.md:202-207`) already describes the current `<mg-generate-drawer>`-mounted Video tab (I2V/FLF/R2V modes) — verified against `static/mg-generate-drawer.js`'s real behavior, no hand-rolled-form description survives anywhere in the file. **~90 min** for the remaining open Invariant clauses above.

**Smaller doc items** (batch into whichever pass touches the file): `build_chat_edit_parameters`' kaisuukenId NOTE is contradicted 19 lines below it in the same function; ~~the `--watch` STATUS paragraph is answered six lines below it and the "future" work it describes shipped~~ **FIXED 2026-07-23**: the STATUS comment above `run_watch` (`pixai_gallery_backup.py:5145-5148`) now describes the shipped state directly instead of framing `_WS_DONE_STATUS`/`--watch-backup` as open future work; ~~`build_shot_video_params`' docstring cites a STATE section that no longer covers the R2V negative-prompt gap (which is now recorded nowhere else)~~ **FIXED 2026-07-23**: the docstring (`:3761-3773`) now states the R2V/V2V negative-prompt API gap directly, with no stale STATE.md citation; ~~`mg-model-picker.js:6` and `mg-gallery-picker.js:6` cite `docs/SUITE_ARCHITECTURE_AUDIT.md`, archived~~ **FIXED 2026-07-23** (commit `d017198`): both now cite `docs/archive/SUITE_ARCHITECTURE_AUDIT_2026-07-13.md, archived`; ~~`mg-generate-drawer.js:14` documents a `tab` attribute it never reads~~ **FIXED 2026-07-23** (commit `2b891df`): usage comment now documents `data-loom-ctx`, the one attribute actually read at mount, and the dev harness's stale `tab="video"` example was removed too; `tests/test_route_tiers.py:211-220` carries a "FINDING (2026-07-19, unresolved)" that STATE has since closed; ~~the Your Art panel blames a nonexistent localhost gate for a failed view fetch (`:5569`)~~ **FIXED 2026-07-23** — reworded to "load lazily, from any signed-in device" (`pixai_gallery.py:5732`); ~~the layout diagrams in `architecture.md` and `How-It-Works.md` omit nine `out_dir` entries including `view_presets/`, `prompt_snippets.json`, `toolbox_presets.json` and `schedule.json`~~ (`How-It-Works.md`'s copy **FIXED 2026-07-21**; `architecture.md`'s **FIXED 2026-07-22**, 11 entries added, including `toolbox_presets/`'s later same-night per-account split) — plus the Pixeltable store outside `out_dir` entirely, still undocumented; ~~`How-It-Works.md:15` says `--watch`/`--claims` are CLI-only (both have web surfaces, documented on three other wiki pages)~~ **FIXED 2026-07-23** (web equivalents named: live-mirror status dot for `--watch`, header claim button for `--claims` — see the `wiki-truth` appendix row below); `Generating.md`'s auto-fallback claim sits under the web-drawer heading while `Troubleshooting.md:26` says the opposite; ~~`Setup.md:18` lists `cryptography` in the requirements table but it's not in `requirements.txt` (→ silent HTTP fallback → no service worker)~~ **FIXED 2026-07-23** (see the `wiki-truth` appendix row below); `config.example.json:24,27` say AUTH_USERS is "managed ONLY via the CLI" and login gates "every non-localhost request" — both false; it omits `AUTH_EPOCH_SEQ` (~~along with `ARTWORK_LIST_HASH`/`ARTWORK_DETAIL_HASH`~~ — those two **FIXED 2026-07-22**, same commit as the appendix `schema-config-drift` duplicate: both added with placeholder values; `AUTH_EPOCH_SEQ` alone remains omitted, still open); `moonglade_mcp.py:8,12` leak `C:\Users\gwilkins\...` in copy-pasteable setup instructions inside a **core module**; ~~`docs/curation_reference_builder.py` is a NameError-on-import script with the owner's Downloads folder, a dead scratchpad GUID~~ **FIXED 2026-07-23** (commit `d017198`): both hardcoded paths replaced with `CURATION_INPUT_DIR`/`CURATION_OUT_DIR` env vars and a clear `SystemExit` when unset — no more NameError-on-import. **Still open:** the doc-that-moved-to-archive citation was NOT part of that fix — the docstring (`curation_reference_builder.py:2`) still points at `docs/CURATION_STANDARD.md`, which does not exist; the real file is `docs/archive/CURATION_STANDARD_2026-07-17.md`; ~~README has no Loom row, no Folio of Honors (renamed from Trophy Hall 2026-07-22), three missing wiki links~~ **FIXED 2026-07-22** (commit `2f9a213` — the same pass that closed the `fresh-clone`/`sweep-bcd` appendix duplicates of this fact; this narrative copy was the one left unstruck): feature table gained a Loom mention and a Folio of Honors mention, wiki index gained The Loom / Control Panel / Folio of Honors links. ~~three orphaned screenshot captions, and a "make it yours" branding bullet that ships zero assets~~ **FIXED 2026-07-23** (commit `d017198`): the three caption-only headings were deleted, and "make it yours" now reads "(none ship — bring your own art)" (Quickstart's "That's the whole setup" omitting account creation **FIXED 2026-07-21** — added a step 4); ~~`.gitignore` generalizes only `jobs.jsonl` beyond the default `out_dir` name~~ (**FIXED 2026-07-23** — see the `schema-config-drift` appendix row below); ~~no `SECURITY.md`~~ **FIXED 2026-07-23** (commit `d017198`): `SECURITY.md` now exists, documenting an email-based reporting path (private vulnerability reporting via GitHub's native feature is still confirmed off — `gh api repos/Nelnamara/moonglade-athenaeum/private-vulnerability-reporting` → `{"enabled":false}`, re-checked 2026-07-23 — the new doc deliberately does not claim that feature is live, routing to email instead); the Desktop stale-checkout warning was lost in the doc consolidation; `wiki/Generating.md:160`'s `--suggest-prompt` example is dead (endpoint is **image-only**, not age-limited — proven by controlled sampling); ~~the video model/duration surface is undocumented (6s duration never mentioned, 7-model roster + `no card` flags + per-model duration caps + per-model **mode gating** that *hides* three of the four documented Shot modes)~~ **FIXED 2026-07-23** (commit `e36976d`): new "Video models and shot-mode gating" section in `wiki/Generating.md`, verified directly against `static/mg-generate-drawer.js`'s `MODELS`/`MODEL_VMODES`/`MODEL_MAXDUR` tables, plus a cross-reference in `wiki/The-Loom.md`'s Shot modes table; ~~`wiki/The-Loom.md`'s Deep Focus inventory omits the base-prompt field~~ **FIXED 2026-07-21**; ~~`wiki/Collections.md` omits the v2.2.0 headline (`− Remove from «collection»`) and the Actions ▾ menu it now lives behind.~~ **FIXED 2026-07-23** (commit `e36976d`): both documented, verified against the real bulk-bar template. Incidental finding while there, NOT fixed (out of scope, flagged for the next pass): the same page's adjacent "Send to Video … up to 9" claim is stale — the real cap is 6, per B18's already-shipped `bulkSendVideo` fix.

---

### Tier 5 — Test gaps (10 items)

The suite has **no** zero-assertion tests, no swallowed assertions, no xfails, no permanent skips. The defect is confined to one idiom — substring-in-a-blob — and to coverage holes:

1. ~~**`test_wildcard_bind_addresses_probe_loopback` asserts exactly the value the bug produces.** `tests/test_port_preflight.py:108` `assert port_owner(host, _free_port()) == ""` — a *skipped* pre-flight returns `""`. Proven: delete the rewrite and all three params still pass. `'::'` and `''` are covered by nothing. This is the guard against becoming the second server on a live port, which has burned this project twice.~~ **FIXED 2026-07-22**: duplicate of the appendix's `tests-that-dont-bite` row (struck there, missed here). `tests/test_port_preflight.py` now spins up a real server and asserts `_port_owner_settled(host, port) == "moonglade"` for the `0.0.0.0`/`::`/`""` params, instead of asserting `""` against a merely-free port.
2. ~~**Nine assertions satisfied by comments, CSS selectors, or prefix collisions** — each proven by running the mutation the test's own docstring says should break it: `lv-overrideflash` (a `//` comment), `c.prompt` (a JSX comment *and* `c.promptOverride`, i.e. it passes in exactly the world the test forbids), `anim-classic` ×2 (the `:not(.anim-classic)` CSS rule), `anim-eclipse` + `mk-tile` (CSS on a default page with neither applied), `"2.5"` (a `font-size:12.5px`), `#gen-drawer.wide` (an `or` whose first disjunct is a superstring of the second, landing on the **desktop** rule), and the service-worker `thumb_branch` slice (which evaluates to `');\n'`). Generalization the lenses converged on: **an assertion about a rule inside a media query must be scoped to the media-query slice, not the page** — that one rule fixes all four of `test_portrait_mobile_pass`'s assertions at once, and mobile portrait CSS is the thing memory records as RESOLVED 2026-07-19.~~ **FIXED 2026-07-22**: all seven underlying tests were already retightened (see the matching `tests-that-dont-bite` appendix rows, each already struck FIXED) — this Tier-5 summary line just never got struck along with them. Verified against current source: `tests/test_loom_notify.py` matches the real rendered `lv-overrideflash` div and exact `value={c.prompt ` binding; `tests/test_branding.py` asserts the mark span's own class attribute; `tests/test_web_pick.py` isolates the mobile media-query slice, asserts the literal V4.0 warning text, and slices the service worker's thumb branch off a real marker.
3. ~~**Nothing tests `_MIGRATIONS` against `CATALOG_FIELDS`** despite the docs calling it a three-place contract.~~ **FIXED 2026-07-23** (commit `2040fa9`): new test drives a real sqlite table containing only the original 18-commit-cc2aeb1 fields through the real migration path, asserting every current `CATALOG_FIELDS` column exists afterward. Searched git history for a real historical instance of the bug (a field added with no matching migration in the same commit) — found none across all 9 later schema-adding commits, so this is genuine future-proofing, not fixing a past incident.
4. ~~**The byte-safe branch of `--organize` is untested.**~~ **FIXED 2026-07-23** (commit `2040fa9`): two new tests cover both the identical-bytes-dropped and differing-content-kept-side-by-side cases, each independently confirmed non-vacuous by an adversarial reviewer who forced the branch both ways and watched the right test fail each time.
5. ~~`tests/test_read_only.py`'s docstring states the false premise as fact; no CLI-entry-point test exists (see S1).~~ **FIXED** (as part of S1, 2026-07-21): `tests/test_read_only.py`'s docstring has been rewritten to describe the real split ("This file covers the four WEB choke points... see tests/test_read_only_cli_paths.py for that half"), and `tests/test_read_only_cli_paths.py` exists with 10 CLI-entry-point tests. This item was just never struck when S1 landed.
6. ~~`tests/test_panel.py`'s FakeProc writes `./HEAD` (see O10).~~ **FIXED 2026-07-23** (commit `1ba9505`): both `FakeProc`s in `tests/test_panel.py` now guard `if argv and argv[0] == "ffmpeg":` before writing, and `.gitignore`'s `/HEAD` comment already documents the real fix. See O10 (Tier 3) above.
7. ~~`test_cost_badge_ships_with_every_price_surface` claims "every surface" in its docstring and asserts two ids and two script tags — it cannot see the hand-rolled Enhance confirm.~~ **FIXED 2026-07-23** (commit `119326b`): stale on both counts — the docstring no longer claims "every surface" (rewritten to scope itself to the `<mg-cost-badge>`/script pairing and name the Loom's two non-badge cost surfaces as deliberately uncovered), and the "hand-rolled Enhance confirm" it supposedly can't see no longer exists: D-12 removed the Enhance tab's `window.confirm()` entirely in favor of a badge.
8. **`loom/src/loom-core.js`'s `tallyPrices`/`formatCostEstimate`/`costTooltip` have zero test references** despite carrying an explicit spend-path invariant in their own comment (a displayed "0 cr"/"free" must only ever mean a genuinely settled zero-cost result). An untested spend-path invariant outranks a weak assertion. **PARTIALLY FIXED 2026-07-23**: `tallyPrices` and `formatCostEstimate` are now covered by `loom/test/loom-core.test.js:251-273`, including a test asserting a settled zero-cost result reads "0 cr" only when genuinely paid-and-settled, never merely unpriced/pending — the exact invariant this item names. ~~`costTooltip` alone still has zero test references anywhere in the repo.~~ **FULLY FIXED 2026-07-23** (commit `8b0452b`): 5 new tests cover free/settled, paid/settled, and unpriced/pending states, following the exact pattern of its already-covered siblings. Adversarial review deleted the pending-clause ternary and confirmed exactly the 2 pending-dependent tests failed, the other 3 stayed green.
9. ~~No test drives `run_generate`/`run_edit_image` against a composite-grid fixture — which is why B3 ships green. The fixture already exists at `tests/test_model_grid.py:174`, pointed at the collect path.~~ **FIXED 2026-07-23**: `tests/test_model_grid.py:220-244` — `test_run_generate_saves_batch_individuals_not_the_grid` and `test_run_edit_image_saves_batch_individuals_not_the_grid` now drive both CLI runners against the composite-grid fixture end to end and assert only the real per-image ids get downloaded. Matches B3 above (Tier 2), which is also FIXED but was separately missed on its own board row.
10. `tests/test_pure.py:118-121` locks in the INVARIANT 1 break (see P7).

---

### Tier 6 — Features (backlog, not defects)

**Blocking-first:** ~~persist `paid_credit` — it's fetched on every poll (`_GEN_STATUS` asks for it) and thrown away on every run; no catalog column, no job-event field. Every downstream ask (spend charts, cost-per-model, the Loom's project ledger) is blocked on it, and each day it isn't stored is history that can't be recovered.~~ **SHIPPED 2026-07-23** (branch `paid-credit-persist`, data layer only): catalog column `paid_credit` via the three-place contract; stored at every capture site (generate/edit/video runs, `collect_generation` — which covers the web poll, `--watch-backup`, and `--task-id` recovery — `--sync-videos`, and the full-meta mapping). The "history that can't be recovered" fear turned out false: the task-detail record returns the cost for historical tasks, so `--backfill-full-meta --with-credit` recovers old spend. UI (spend charts, Loom ledger) still open — this unblocked it, it didn't build it.

**Curator, owner-ranked:** Triage Deck (#1 — the lightbox keydown handler binds only arrows/Escape/`f`/space; no rate, collect or delete keys); saved views usable in the Picker (#2 — the stored-query half shipped, the consumption half didn't); ~~**field operators in search (#3 — highest value-per-hour in the list: 14 populated columns are unreachable and `_build_where` just never learned `key:value`)**~~ **SHIPPED 2026-07-23** (commit `99825cd` — see the matching `sweep-bcd` appendix row); near-dup clustering (#4 — the expensive half, a populated CLIP index, is already built); stack-by-batch (#5 — pure rendering, the grouping key already exists and is already queryable); sibling preview on cloud delete (#6); **trash browser for `_deleted/` (#7 — the delete confirm promises "recoverable" and no restore path exists)**; collection rename/merge/delete (#8); integrity job (#9 — the cheapest piece, `uncataloged = on_disk_ids - catalog_ids`, is two lines against sets `collection_health` already holds).

**Loom:** per-shot takes (#2 — four completion sites clobber `resultMid`, not two, and Draft mode's own tooltip *instructs* the destructive re-roll); spend ledger (#5 — blocked on `paid_credit`; note cost-to-*finish* already ships as the toolbar pill, only the spent half is missing); re-anchor warnings (#7); player audio (#8, rescope: both Loom players are hard-muted while the export deliberately muxes audio — the music-bed half is superseded by the out-of-scope multi-track decision); editor handoff export (#9 — "finally makes those fields DO something").

**Power-user:** Remix from the lightbox; model/LoRA favorites (clone the `/api/snippets` pattern); card-utilization digest (mostly shipped — only per-model coverage and expired-unused remain); prompt matrix (defer); contest shortlist (parked behind Epic C); metadata recovery for hand-made folders (adoption item).

**Untriaged:** the SWEEP §D 22-item slate that `docs/archive/` points at and no live tracker carries — ~9 shipped, ~13 survive, none of them tracked anywhere. One triage pass, then delete the rest explicitly. `mascots.json` should probably just be dropped — `docs/ART.md:124-127` already *is* the asset-to-role registry, including the non-achievement roles that were the only argument for building one.

---

## 4. Already true / already fixed — delete these from any tracker

Deleting stale entries is real work; each of these will otherwise be re-derived at full cost.

**The two canonical "no way in" bugs are genuinely closed.** `/collection-remove` has a real caller (`pixai_gallery.py:4689`, correctly gated `{% if collection %}`) plus a UI-path test. `/export-csv`'s filtered link exists at `:4114` carrying `request.query_string`, with the whole-catalog dump deliberately scoped to the Panel — and `tests/test_web_pick.py:690` pins both halves so it can't revert.

**Six of the eight fresh-clone defects in `SWEEP_2026-07-16.md` §C are dead:** the five broken README embeds are gone; LICENSE exists and GitHub reports MIT; master carries CHANGELOG and `__version__ = "2.2.0"`; `websockets` is in requirements; the no-config error points at a wiki page that exists; `test_similar.py` `importorskip`s pixeltable so a plain `pytest` is green. Only the wiki drift (C3) and Loom visibility (C8) survive.

**The frozen roadmap is mostly false now:** the `/api/loom/export` `a=0` audio drop is fixed (it maps both streams and pads silence with `anullsrc`, pinned by a test asserting `"a=0" not in fc`); all eleven V1→V2 convergence items shipped; four of the nine ranked Loom items shipped (persist task_id, draft quality, trim-aware handoff, project Look — all verified reachable, not merely present); the achievements roster is 57/57 on both `badge` and `art_candidate` and `LE10_Q3` is no longer a spare; the F#/G#/Z# "owner call" was **resolved inside the same document** and must not be re-raised; the "rebuild the roster board" HOLD has no work left in it; the PySide6 "surgical slimming, NOT a wholesale delete" plan was superseded by the wholesale delete; `docs/ROADMAP.md`/`docs/REFINEMENTS.md` were deleted in a clean consolidation with every queued item carried forward; Footage's "open fork" was decided and shipped as a tab.

**Also verified fixed / never true:** `/api/panel/status` stdout redaction is done *and* has a companion test proving it isn't over-gated; `docs/architecture.md`'s localhost-spend claim is corrected and now names `test_route_tiers.py` as the tie-breaker; the `.sb-pick-ov` / `mg-gallery-picker` z-400 tie is closed at 500 on both sides; every documented CLI command exists and dispatches (a full reverse-diff of all 118 flags against every doc found zero dropped commands, and all 20 `PANEL_ACTIONS` argv entries resolve); the GUI cut left no import/dependency/CI/packaging/`.gitignore`/test residue; `config.example.json` leaks no real credential and no tracked file contains this machine's key, secret, or password hashes; `.gitattributes` + `core.autocrlf=true` are settled with zero phantom diffs; `frameLinked`'s **unit tests** are green (which is the finding, not the reassurance); `--reference-video`'s "documented 15s default" gotcha is a code-vs-code issue, not a doc issue; the near-dup "same media_id twice in k slots" concern is impossible (`pixai_similar.py` primary-keys on `media_id` and dedups before insert, on purpose); the `AUTH_EPOCH_SEQ` "walks back through 1,2,3" hazard was fixed by the 1,000,000 legacy margin three lines above the cited line; `#suggest-box` is not a dangling id (created at runtime); `wiki/Folio-of-Honors.md`'s (renamed from `Trophy-Hall.md` 2026-07-22 — same content, re-verified at the new path) entire 57-achievement roster, thresholds, tiers, point formula and skin table verify exact.

**One verdict, already reversed — this row just didn't catch up:** `docs/STATE.md:422-424`'s "only write site for `c.prompt`" was filed ALREADY_FIXED by one lens; at the time this section was first written, the **code** had shipped (`jsx:1588`, in the built bundle) but the **doc** was still stale at HEAD. ~~Keep it as open doc work (P3).~~ **FIXED 2026-07-23**: P3 (Tier 4, above) already struck this exact citation as fixed on 2026-07-22, and `docs/STATE.md` (now ~line 504-509) reads "There are now **two write sites for `c.prompt`**", correctly describing the shipped behavior. Both halves — code and doc — are settled; this section-4 copy is the one that never got updated to match P3's strike.

---

## 5. What the audit missed

Pooling the refuters' answers plus my own. The pattern across all sixteen lenses is the same: **each asked its question once and stopped, instead of running it to closure over the whole class.**

- **The single biggest miss was a regression, not a gap.** The gallery Video tab's lost Job Tracker registration (§1) was found by *one* refuter, not by any lens. The orphaned-surfaces lens audited component APIs **globally** ("does anything, anywhere, listen for `mg-cost`?") and got the inverse result — it flagged an optional presentational hook with no consequence and missed a real dropped capability. The right question for a partial migration is **per-mount**: for each host of a shared component, which events does *that host* consume, and what did it lose relative to its pre-migration code? Run that matrix and gallery-1-of-14 vs Loom-14-of-14 is the first row.
- **A second cheap heuristic nobody applied:** for any function deleted in a migration commit, grep the surviving comments that *name* it. `Gen.videoGenerate` is gone from the code and still cited as live precedent in the Loom.
- **Reachability was asked of defects but almost never of remedies or of the codebase as a whole.** No lens enumerated `@app.route` endpoints against `url_for`/literal `fetch()` references — one script closes "reachable from the UI?" for all 86 routes at once. (It comes back clean, which is a *stronger* result than "I checked the two I already knew about.") Conversely, nobody asked the inverse question — **which LOCALHOST-gated controls are rendered to every LOGIN session?** There are three: ↑ Import (`:3911`, inside the always-true `{% if is_local %}`), "Set launcher icon" (`:7319`, on a page that passes no locality flag at all), and every destructive Panel action button — each of which walks a LAN user through a confirm dialog before 403ing.
- **Class-level sweeps were done by anecdote.** `--variant` was found by reading an archived HTML dashboard; a five-line AST pass over all 115 argparse dests settles the whole class and proves it's the *only* dead flag. Same for the `not in` mirror of the substring idiom (93 assertions — clean, but "I checked and it's clean" was owed and never delivered), and for the auth path itself (`_is_authorized_request` has no try/except so a raise denies; `_load_config` returns `{}` on corruption so a bad config denies everyone — verified clean, but on the day four security fixes landed, "swept, fails closed, here's why" is what a reader needs to distinguish from "never looked").
- **Under-counting blast radius, repeatedly:** four `resultMid` clobber sites (not two), four `_apply_kaisuuken` call sites (not three — the omitted one is the video path), five `_deleted/`-blind walks including `cmd_organize` (not four), both Loom players muted (not one), all three `ENHANCE_PLUGINS` entries dead (not two), six lightbox buttons (not three), 11+ tree walks (not seven). Three proposed fixes were mis-sized as a result, and one (`_task_image_media` for `run_generate`) would have silently dropped video recovery.
- **Sibling files went unread.** One lens indicted `docs/archive/DASHBOARD_2026-07-20.html` without opening `docs/archive/README.md`, which (a) already records the remediation and (b) *forbids* the proposed edit. Another recommended building a `--convert-existing` Panel button that the owner explicitly answered "superseded" on 2026-07-15, with the replacement shipped 2026-07-20 — the memory-flagged "don't recommend superseded designs" mistake, restated as a finding. Reading `git log` on the commit an audit's own evidence names should be routine before recommending a deletion or a build.
- **Doc-trust crept back in.** Several "verified clean" claims rest on `docs/STATE.md` saying so, in an audit whose governing rule is that the docs go stale faster than the code. They happened to be right; the method was wrong. Three cross-reference `file:line` anchors in one lens didn't resolve at all.
- **My own additions:** `moonglade_mcp.py` leaks the owner's absolute paths in a **core module's** copy-pasteable setup command (missed while another lens asserted the `docs/` script was "the only committed file with an owner-machine path"); `config.example.json` — the one file the Quickstart tells every stranger to copy — describes account management and the auth model wrongly in three clauses; ~~the layout diagrams omit four stores of irreplaceable user-authored content~~ **FIXED 2026-07-23** (already struck in P7/Tier 4 above on 2026-07-22, missed here) — `docs/architecture.md`'s layout diagram now lists `prompt_snippets/`, `view_presets/`, `toolbox_presets/` and `schedule.json` among the entries added; this retrospective line in §5 just wasn't updated to match; and every lens audited `loom-v2` while a fresh clone gets `master` (identical but for `docs/STATE.md` — never established by anyone).

---

## 6. Realistic totals

| Tier | Open items | Notes |
|---|---:|---|
| Security | **0** | closed 2026-07-23 (S5 was the last one) |
| User-visible breakage / data loss | **0** | all 18 (B1–B18) closed 2026-07-23; B7 was already decided-intentional, not a gap |
| Orphaned / half-migrated | **~3 standing notes** | O2/O5/O7/O9/O11 closed 2026-07-23; O12–O14 are architectural observations (picker-parity drift, an unused public-API surface), not scoped defects with a fix — no time estimate applies |
| Doc lies | **0 of the 7 named passes** | P1–P7 all closed (P3/P6/P7 + smaller items closed 2026-07-23); a handful of low-severity appendix one-liners below may still be open — not individually re-swept this pass |
| Test gaps | **0** | all 10 closed 2026-07-23 |
| **Subtotal (real work)** | **effectively closed** | everything with a time estimate in Tiers 1–5 as of 2026-07-21 is now fixed; what's left is either a standing note (not a defect) or an unswept appendix long-tail — a fresh audit pass would be needed to say "0" with full confidence, this board was not re-run from scratch |
| Feature backlog | ~30 | Not defects; needs triage (D-7, F1) before estimating — untouched by this sweep |
| Decisions | 13 | Blocks ~10 of the above — untouched by this sweep, see STATE.md's Open Owner Calls |
| **Already-fixed / never-true — delete** | **~40** | ~1 hour of doc pruning, high leverage — untouched by this sweep |

**2026-07-23 sweep note:** the entire security tier (S1–S5) and every scoped Tier 2–5 item with a time estimate closed across two sessions today — S5 individually, then B9/B10/B11/B14/B15/B16/B17/B18, O2/O5/O7/O9/O11, P3/P6/P7 + 2 smaller doc items, and all 3 remaining test gaps via a 7-batch parallel sweep (each batch: isolated worktree, fail-first TDD, independent adversarial review before merge — 5 of 7 batches approved clean, 2 approved with a note; one real one-line doc fix from review was applied before merging). Full suite: 912 Python / 149 Node, both green. Not done: a fresh 16-lens re-audit (this sweep worked the existing board's named items, it did not go looking for new ones), the Tier 6 feature backlog, the 13 open Decisions, and the ~40-item doc-pruning pass.

---

## Appendix — every surviving finding, one line each

The board above dedupes and promotes. This is the raw survivor list, so nothing the
consolidation merged can quietly disappear. Sorted by severity, then lens.

| sev | lens | category | est | finding |
|---|---|---|---:|---|
| high | `auth-tier-drift` | doc-lie | 10min | ~~wiki/Generating.md tells users the web Generate drawer is localhost-only; it is LOGIN, so any signed-in LAN device can spend the owner's credits~~ **FIXED 2026-07-21** (duplicate of the `wiki-truth` lens's identical finding, fixed in the wiki-truth sweep — this row was missed at the time) |
| high | `cli-surface` | security | 45min | ~~READ_ONLY is bypassed by all five CLI create paths — --generate/--generate-video/--reference-video/--enhance/--edit-image fire createGenerationTask directly~~ **FIXED 2026-07-21** (S1) |
| high | `fail-open` | defect | 45min | ~~A transient free-card check failure silently spends real credits after the badge promised "0 credits"~~ **FIXED 2026-07-22**: `match_kaisuuken(raise_on_error=True)` + `_apply_kaisuuken` now retries the check once, then ABORTS the submission with a clear `PixAIError` instead of falling through to "no card -> pay credits." Covers the CLI (5 submit call sites) and all four web routes — `/api/generate`, `/api/edit`, `/api/enhance`, and `/api/loom/generate` (the video path B5's counterpart entry specifically warned would be missed) — since the retry/abort logic lives in the shared `_apply_kaisuuken` choke point itself (`pixai_gallery_backup.py:5670-5717`), not per-route; verified all nine call sites funnel through it. See B5 above (Tier 2), which describes this same finding and is now struck to match. |
| high | `fail-open` | defect | 30min | ~~A LoRA whose version lookup fails is silently dropped from a paid generation; the only signal is an hourglass that reads as "loading"~~ **FIXED 2026-07-22**: the failure path (`.catch`) and an empty successful response are both now marked `entry.failed`, the chip shows a distinct warning icon + tooltip instead of an eternal hourglass, Generate is disabled while any added LoRA is unresolved, and `generate()` refuses to submit even if the button state is bypassed. |
| high | `fresh-clone` | doc-lie | 25min | ~~The published GitHub wiki — README's "full documentation" — is 4 releases stale and still tells new users to run the deleted PySide6 GUI~~ **FIXED 2026-07-22** (P1/D-10) |
| high | `frozen-roadmap` | orphaned | 150min | The unlock toast tells users "unlocks a banner" and nothing in the app ever unlocks a banner |
| high | `half-migrated` | half-migrated | 15min | ~~Gallery Video tab never listens to mg-submit, so its generations vanish from the Job Tracker and never refresh the credit chip~~ **FIXED 2026-07-21** (B4) |
| high | `invariants` | defect | 25min | ~~INVARIANT 3 is broken on the only resume path that runs: run_download's disk index never checks file size, so a zero-byte image is never re-downloaded~~ **FIXED 2026-07-21** (B1) |
| high | `invariants` | defect | 30min | ~~INVARIANT 3 also unguarded in the audit: a zero-byte file can be chosen as the dedup KEEPER, sending the real image to quarantine (or to unlink() under --dedup-delete)~~ **FIXED 2026-07-21** (B2) |
| high | `orphaned-surfaces` | defect | 45min | ~~is_lora_compatible() -- the LoRA architecture-mismatch spend gate -- has zero production callers; only tests exercise it~~ **ANSWERED 2026-07-22 (D-2)**: deliberately left unwired — see B7 above. |
| high | `state-owner-defects` | security | 25min | ~~Known-defect "/panel lists every account username" understates the hole: any LAN account can also CREATE and DELETE accounts~~ **FIXED 2026-07-22**: `api_users_add` moved to LOCALHOST; `api_users_remove` refuses removing anyone but the caller's own account unless the request is local (self-removal was the owner's explicit carve-out, decided the same day the fix was requested). Username *visibility* on `/panel` is unchanged — reading the roster was never the part of this finding that was open. |
| ~~high~~ | `state-owner-defects` | security | 0min | ~~Cache Storage outlives sign-out — confirmed exactly as documented, cache-first with no revalidation on /img/ and /full/. **A fix was drafted 2026-07-21 and REJECTED for two falsehoods — do not re-draft it:** purging from the login page assumed "whoever is looking is unauthorized," but `login()` has no signed-in short-circuit, so a bookmark/Back press would wipe a signed-in user's whole thumbnail cache; and its wiki wording was false on the documented `--host 0.0.0.0 --https` path, where no worker registers at all. Settle the PWA-verification item (D-9) first — it may moot this entirely.~~ **FIXED 2026-07-22** — see S4 above. |
| high | `sweep-bcd` | orphaned | 45min | ~~ENHANCE_PLUGINS has zero callers — the `plugin=` branch of /api/enhance is dead and hand-fix/face-fix are unreachable~~ **DELETED 2026-07-22**: the Enhance UI only ever sends `workflow_id`, never `plugin` — confirmed no test or client code sent it. hand-fix/face-fix are superseded by the real, working box-based `/api/fix` (submit_fixer); detail-fix's workflow is reachable the normal way already. Unlike `is_lora_compatible` (line above), this one really was just dead, not a gap to wire in. |
| high | `tests-that-dont-bite` | test-gap | 15min | ~~test_wildcard_bind_addresses_probe_loopback asserts the exact value the bug it names produces~~ **FIXED 2026-07-22**: it probed a FREE port and asserted `""`, which a wildcard host returns whether or not `port_owner`'s rewrite to `127.0.0.1` happens (a connection to a free port refuses either way) — could not catch the rewrite being deleted. Rewritten to probe a live server through the wildcard host and require it to be recognised as `"moonglade"`; verified by temporarily deleting the real rewrite and confirming all 3 parametrized cases fail. |
| high | `unfiled-workflow-findings` | defect | 45min | ~~CLI --generate/--edit-image with batch>1 catalog only the composite grid; the individual images are silently never downloaded~~ **FIXED 2026-07-22**: both now build their media list via `_task_image_media` (the same helper `_download_image_task`/`web_generate` already used correctly) instead of reading `outputs.mediaId`/`batchMediaIds` directly, which read the composite GRID id and a field that's null on modern tasks. Also picks up each image's own per-batch seed instead of the shared submitted seed. |
| high | `wiki-truth` | security | 45min | ~~READ_ONLY does not block ANY CLI generation path — Trust & Safety's central guarantee is false~~ **FIXED 2026-07-21** (S1) |
| high | `wiki-truth` | doc-lie | 10min | ~~Generating.md still says the Generate drawer is "localhost-only" and that LAN visitors "can look" without signing in~~ **FIXED 2026-07-21** |
| medium | `auth-tier-drift` | security | 45min | ~~/api/jobs hands a LAN caller the verbatim, untruncated exception text from a terminal CLI run — including absolute host paths and the OS username~~ **SIZE-CAPPED 2026-07-21** (S3, write-side): `append_job_event` now caps every string field at 200 chars, closing the "no limit at all" half. A short message can still contain a host path — this is NOT the broader redaction fix; see the still-open row below. |
| ~~medium~~ | `auth-tier-drift` | doc-lie | 15min | ~~Four route docstrings still declare a tier the code does not enforce, including two "Localhost" claims on LOGIN spend/read routes~~ **FIXED 2026-07-22**: `api_ping`, `api_model_search`, `api_branding`, `loom_generate` all corrected to LOGIN, matching `tests/test_route_tiers.py`'s `ROUTE_TIERS`. |
| medium | `auth-tier-drift` | doc-lie | 10min | ~~docs/STATE.md says "Only five routes are LOCALHOST" — there are seven, and the two it omits are cloud deletion and host-filesystem writes~~ **FIXED 2026-07-22**: corrected to the true count (now eight, api_users_add having joined the tier the same day) and pointed at ROUTE_TIERS as the source of truth instead of restating a number that will drift again |
| ~~medium~~ | `cli-surface` | doc-lie | 20min | ~~--full-meta's own --help tells users to capture TASK_DETAIL_HASH + MODEL_DETAIL_HASH into config.json, contradicting the shipped-defaults promise; its three enforcement guards are unreachable and point at a README section that does not exist~~ **FIXED 2026-07-22**: all four guard messages + the `--full-meta`/`--delete-task` help text rewritten to say the hashes ship with working defaults; the nonexistent "README -> Full Meta" citation replaced with the real RECAPTURE section. |
| ~~medium~~ | `cli-surface` | orphaned | 35min | ~~--enhance is a working, credit-spending create path that appears in no current doc~~ **FIXED 2026-07-23**: new "Enhance an image" section in `wiki/Generating.md` documents `--workflow-id`/`--filter-id`/`--strength`, the preview/`--confirm` gate, and explicitly notes it has no cost-preview (unlike every other spend command) since PixAI's own pricing endpoint doesn't cover this task family. (The `--filter-id` web-route half of this finding is a separate, real gap — art filters have no discovery UI even though the CLI flag works — not addressed here.) |
| ~~medium~~ | `fail-open` | defect | 40min | ~~A backup that failed N downloads reports "✓ finished (exit 0)" in the Panel and a green "done" in the Jobs card~~ **FIXED 2026-07-22** — see D-4: real `done_with_errors` status, visible and dismissable everywhere a job shows up. |
| ~~medium~~ | `fail-open` | defect | 30min | ~~/export-zip discards every convert/embed status note, so "export as JPEG + embed prompt" can silently ship untouched originals~~ **FIXED 2026-07-23** — see B12 above: convert/embed failures now collect into `_export_warnings.txt` inside the zip plus an `X-Export-Warnings` header. |
| ~~medium~~ | `fail-open` | defect | 15min | ~~Prompt-snippet saves are fire-and-forget against an endpoint that returns HTTP 200 with an error body on write failure~~ **FIXED 2026-07-23**: `Snips.persist()` now checks the response and shows a `Toast` error on write failure (`pixai_gallery.py:5919-5930`). |
| ~~medium~~ | `fail-open` | defect | 20min | ~~A transient PixAI blip on /api/task-status is reported to the browser as a terminal failure and the poll loop gives up~~ **FIXED 2026-07-23** — see B13 above: the transient branch now answers `phase:"running"`, not `"failed"`. |
| ~~medium~~ | `fresh-clone` | doc-lie | 15min | ~~README's documentation index and feature table omit The Loom, the Control Panel wiki page, and the Folio of Honors (renamed from Trophy Hall 2026-07-22) entirely~~ **FIXED 2026-07-22**: the wiki-index line (not the in-repo docs line, which already linked `docs/LOOM.md`) gained The Loom, Control Panel, and Folio of Honors links; the feature table gained a Loom mention on the Generate row and a Folio of Honors mention on the Curate row. |
| ~~medium~~ | `fresh-clone` | test-gap | 10min | ~~tests/test_port_preflight.py is timing-flaky: it failed a full-suite run on a healthy checkout, then passed the next one~~ **FIXED 2026-07-23** (commit `119326b`): added `_port_owner_settled()` (retry with backoff, up to 6 attempts) and swapped it into both live-server-probing tests — the single fixed 0.4s `port_owner()` timeout could lose the race against full-suite thread/GIL contention right at suite start. |
| ~~medium~~ | `fresh-clone` | doc-lie | 5min | ~~CHANGELOG.md's maintenance note states the release state wrongly in both directions — the exact drift CLAUDE.md forbids transcribing~~ **FIXED 2026-07-22**: version corrected to v2.2.0 (verified via `gh release list`); v1.11.0 "never reached master" corrected to "reached master as part of v2.0.0" (verified via `git merge-base --is-ancestor`). |
| ~~medium~~ | `fresh-clone` | doc-lie | 5min | ~~pixai_gallery.py's canonical auth docstring says the opposite of the shipped first-run: "there is no bootstrap/first-run bypass"~~ **FIXED 2026-07-22**: `_is_authorized_request()`'s docstring now describes the real shipped `bootstrap_mode` flow in `login()`, while keeping the true, narrower claim that `_is_authorized_request()` itself has no bypass. |
| ~~medium~~ | `fresh-clone` | orphaned | 10min | ~~docs/curation_reference_builder.py is committed to a public repo but hardcodes the owner's Downloads folder and a dead Claude scratchpad GUID~~ **FIXED 2026-07-23** (commit `d017198`, see the P7 "smaller doc items" paragraph above) — both replaced with env vars. |
| ~~medium~~ | `fresh-clone` | doc-lie | 10min | ~~README's Quickstart claims "That's the whole setup" but omits the mandatory account creation that v2.0.0 made step one~~ **FIXED 2026-07-21** — not "already correct": commit `52ff24b` ("Wiki truth sweep", 2026-07-21 23:48) added step 4 ("First time opening the web gallery: sign in from the machine running the server…") ahead of the "That's the whole setup" line, specifically to cover this gap. This is a duplicate of the P7 "Smaller doc items" entry above (`Quickstart's "That's the whole setup" omitting account creation FIXED 2026-07-21 — added a step 4`), which correctly credits the fix; this row's 2026-07-22 re-check found step 4 already present and wrongly concluded the finding had never been true. |
| medium | `frozen-roadmap` | feature | 45min | The roadmap's "three grounded feature slates" (22 candidates) never landed in any live tracker |
| ~~medium~~ | `gui-removal-residue` | doc-lie | 5min | ~~The /duplicates page tells users to run Dedup from the deleted GUI's Utilities tab~~ **FIXED 2026-07-22**: now links to the Control Panel's real dedup-apply/dedup-dry actions. |
| ~~medium~~ | `gui-removal-residue` | orphaned | 10min | ~~run_audit(), run_dedup() and run_verify_dupes() are dead — the GUI was their only caller~~ **FIXED 2026-07-23**: all three deleted from `pixai_gallery_backup.py` (commit `a4a3de7`; see O6 above); confirmed zero remaining references anywhere in the repo. |
| ~~medium~~ | `gui-removal-residue` | doc-lie | 15min | ~~STATE.md claims nothing is CLI-only, but the GUI's Convert tab (--convert-existing) has no web route~~ **FIXED 2026-07-22**: STATE.md's "were the last CLI-only maintenance actions" false claim removed; `--convert-existing` and `--backfill-meta`/`--backfill-full-meta` now listed as the real remaining CLI-only actions. |
| ~~medium~~ | `half-migrated` | orphaned | 20min | ~~data-loom-ctx has zero callers, so the Loom's Video panel renders two Camera controls and two quality controls~~ **FIXED 2026-07-23** — see O3 above: `master-storyboard.jsx:1419` now sets `data-loom-ctx=""`, activating the existing CSS hide rule; regression test `loom/test/loom-gen-drawer-loom-ctx.test.js`. |
| ~~medium~~ | `half-migrated` | doc-lie | 10min | ~~v2.2.0 CHANGELOG claims every cost display is one component and names a 'picker' cost surface that does not exist~~ **FIXED 2026-07-22** — same commit as the `schema-config-drift` duplicate below (doc-truth-up sweep); "and the picker" dropped, verified via grep that no cost-display "picker" claim remains. |
| medium | `half-migrated` | half-migrated | 240min | ~~mg-model-picker is a strict subset of the #model-flyout it was built to replace, so the gallery can never adopt it and the Loom's Image tab is stuck on a reduced picker~~ **NARROWED 2026-07-22 (D-11)**: the Loom's own LoRA gap is fixed (mg-model-picker gained multi-select + version resolution). The 240min full-Gallery-adoption estimate was always for the LARGER parity question, not this slice — still open, unaffected by this fix. |
| ~~medium~~ | `half-migrated` | defect | 25min | ~~The Similar modal's hand-cloned card omits data-nsfw, so Privacy Blur leaves NSFW lookalikes unblurred~~ **FIXED 2026-07-23** — see S5 above: `/api/similar` now projects `is_nsfw` and `Similar.open()` sets `data-nsfw="1"` on the cloned card. |
| ~~medium~~ | `half-migrated` | half-migrated | 30min | ~~A seventh cost surface — the gallery's Enhance confirm — was never counted, and its free-card branch contradicts its own comment~~ **MOOT 2026-07-22** — see D-12 Increment 1: the Enhance tab's `window.confirm()` was removed entirely (reshaped to select-then-run + persistent badge), so there's no confirm branch left to contradict anything. |
| ~~medium~~ | `half-migrated` | defect | 10min | ~~bulkSendVideo still sends 9 images to a drawer that caps at 6, silently dropping the extras~~ **FIXED 2026-07-23** — see §1 above: now passes the full selection and shows a Toast warning naming the dropped count. |
| ~~medium~~ | `invariants` | doc-lie | 90min | ~~INVARIANT 7 ("one shared media-id -> file matcher") is false as written~~ **FIXED 2026-07-23** (commit `e36976d`, see P7 above) — corrected to name the real independent walkers. |
| ~~medium~~ | `invariants` | defect | 40min | ~~The _deleted/ quarantine is invisible to only one of six bulk walks (five blind)~~ **FIXED 2026-07-23** (commit `a21c20f` etc., see B11 above) — all six now respect it. |
| ~~medium~~ | `invariants` | defect | 45min | ~~already_downloaded() is a guaranteed-False no-op for videos~~ **FIXED 2026-07-23** (commit `8d5b7a9`, see B16 above). |
| ~~medium~~ | `invariants` | defect | 20min | ~~The Loom's last-frame video resolver uses a bare `*<mid>.*` glob with no exact-media-id check~~ **FIXED 2026-07-23** (commit `678d9aa`, see B17 above — the route is `/api/loom/handoff`). |
| ~~medium~~ | `orphaned-surfaces` | doc-lie | 10min | ~~Two shipped UI strings still tell users to use the PySide6 desktop GUI that v2.1.0 deleted~~ **FIXED 2026-07-22**: the "Copy media id" tooltip and every remaining `\bGUI\b` comment/docstring in pixai_gallery.py (found: 5 sites total, not 2 — the audit undercounted) rewritten to describe the real current web surface; 2 of them preserved their historical WHY-shaped-this-way reasoning rather than just deleting the GUI comparison. |
| ~~medium~~ | `orphaned-surfaces` | defect | 20min | ~~clamp_edit_config() guards the web edit path but is never called on the CLI --edit-image path~~ **FIXED 2026-07-22** — see Package 3: clamped in `_edit_config_from_args`, covering both preview and real submit. |
| ~~medium~~ | `orphaned-surfaces` | orphaned | 15min | ~~--variant is accepted, advertised in QUICK START, and read by nothing; its whole helper cluster is dead~~ **FIXED 2026-07-22** — see D-5: flag and its docstring/README mentions deleted, confirmed zero remaining references. |
| ~~medium~~ | `orphaned-surfaces` | orphaned | 30min | ~~/api/view-presets accepts POST {delete: name} but no UI ever sends it -- saved views can be created and never removed~~ **FIXED 2026-07-23** — see O4 above: added a delete-view control wired to the existing route (`deletePreset()`, `pixai_gallery.py:4490-4505`). |
| ~~medium~~ | `schema-config-drift` | doc-lie | 10min | ~~config.example.json omits ARTWORK_LIST_HASH and ARTWORK_DETAIL_HASH, the only two overrides the code tells users to set by name~~ **FIXED 2026-07-22**: both added with placeholder values, matching the style of the other hash entries. |
| ~~medium~~ | `schema-config-drift` | doc-lie | 20min | ~~Both on-disk layout diagrams are missing nine out_dir entries, including the new view_presets/~~ **FIXED 2026-07-22**: docs/architecture.md's layout diagram gained 11 confirmed entries (branding.json, the loom/ subtree, prompt_snippets/, view_presets/, toolbox_presets.json, schedule.json, catalog.csv), each traced to a real `out_dir / "..."` construction in the code. |
| ~~medium~~ | `schema-config-drift` | doc-lie | 5min | ~~The shipped v2.2.0 release notes point users at out_dir/view_presets.json, a path the release itself replaced~~ **FIXED 2026-07-22** — same commit as the `half-migrated` duplicate above; path corrected to `out_dir/view_presets/<account>.json`. |
| ~~medium~~ | `schema-config-drift` | half-migrated | 45min | ~~Only one of three server-side user-content stores got the per-account split; prompt_snippets.json and toolbox_presets.json are still install-wide~~ **FIXED 2026-07-22**: all three now split (prompt_snippets and Loom storyboards fixed earlier tonight as D-7; toolbox_presets.json fixed in this pass, same pattern — `_toolbox_dir()`/`_presets_path(user)`/legacy read-only fallback, cross-account isolation pinned by new tests). Note: this was previously ALSO the last real gap "left alone" by B14's `toolbox_presets.json is import-one-by-task_id and additive — weaker, not a wipe risk" parenthetical — that reasoning was about severity, not a decision to skip the split; it's done now too, and inherits B14's same still-open Windows-filename-case-collision bug. |
| ~~medium~~ | `schema-config-drift` | doc-lie | 15min | ~~CLI --help and two error paths still gate features on config hashes that ship baked-in, and cite a README section that does not exist~~ **FIXED 2026-07-22** — same commit as the `cli-surface` duplicate above (`task_detail_gql`, `run_backfill_full_meta`, `run_download`'s `--full-meta` guard, and the `--full-meta`/`--delete-task` help strings). |
| ~~medium~~ | `schema-config-drift` | test-gap | 20min | ~~Nothing tests that CATALOG_FIELDS, _CREATE_TABLE and _MIGRATIONS stay in sync~~ **FIXED 2026-07-23** (commit `2040fa9`, see Tier 5 above). |
| medium | `state-owner-defects` | orphaned | 30min | The one banner_reward achievement fires a toast promising "Unlocks a banner" and nothing unlocks a banner |
| medium | `state-owner-defects` | doc-lie | 5min | ~~STATE's tier model says "Only five routes are LOCALHOST" — there are seven~~ **FIXED 2026-07-22** (same fix as the `auth-tier-drift` row above) |
| ~~medium~~ | `state-owner-defects` | doc-lie | 20min | ~~PWA-install-on-LAN is promised in three user-facing docs and has never been verified; the code path is werkzeug adhoc self-signed~~ **ANSWERED 2026-07-22** — see D-9: confirmed working, tested live on the owner's iPad Air. |
| ~~medium~~ | `state-owner-defects` | security | 0min | ~~Verbatim-exception LAN disclosure: 38 `str(e)`/`repr(e)` sites in `pixai_gallery.py`... **Re-spin as literal-prefix replacement**...~~ **FIXED 2026-07-22** — see S3 above (Package 5): literal-prefix redaction, adversarially reviewed, spaced-username test case included per this row's own instruction. |
| medium | `sweep-bcd` | half-migrated | 20min | ~~The three wiki pages written today were never pushed to the published GitHub wiki, which is where the README sends every user~~ **FIXED 2026-07-22** (P1/D-10) |
| ~~medium~~ | `sweep-bcd` | doc-lie | 20min | ~~The README's feature table never mentions the Loom or the Folio of Honors (renamed from Trophy Hall 2026-07-22)~~ **FIXED 2026-07-22** — same commit as the `fresh-clone` duplicate above. |
| ~~medium~~ | `sweep-bcd` | doc-lie | 10min | ~~CHANGELOG's maintenance note still says Releases are published through v1.10.0 — they are published through v2.2.0~~ **FIXED 2026-07-22** — same commit as the `fresh-clone` duplicate above. |
| medium | `sweep-bcd` | feature | 180min | `_deleted/` quarantine has no restore path, but the delete confirm promises files are "recoverable". **Scoped + surface DECIDED 2026-07-23**: owner picked a **floating overlay panel** (Folio-of-Honors-style modal over the gallery, the app's native pattern — the Picker already runs an infinite-scroll grid in an overlay), NOT a routed page and NOT Panel-buttons-only. Scoping facts: purge flat-moves files into `_deleted/` and hard-deletes the catalog row + thumbnail, so the panel lists by directory scan with regenerated thumbs (background job; ~12k files live in the owner's quarantine today); media_id always recoverable from the filename, task_id usually; restore = move back + minimal row re-insert + optional metadata re-fetch (getTaskById resolves even deleted tasks); ratings/collections on old purges are gone (no manifest existed — a purge-time snapshot for future deletes is part of the build). Restore = LOGIN tier; delete-forever/empty-trash = LOCALHOST + typed confirm, matching delete-tier precedent. Honest estimate 8-12h, not this row's 180min. **Open: opener placement** (proposed: header utility cluster beside Folio/Health — awaiting owner nod). |
| ~~medium~~ | `sweep-bcd` | feature | 150min | ~~Curator #3 — search has no field operators; seed, negative_prompt, aes_score, liked_count, width and height are cataloged but unreachable~~ **SHIPPED 2026-07-23** (commit `99825cd`): `_build_where` parses `key:value` tokens — text/numeric/date/bool/id operators over every sanely filterable catalog column (all six named here included), quoted values, unknown keys degrading to plain text, free text pinned byte-identical, injection-proven. Grammar in `wiki/Gallery.md` § Search operators. Shared via `_build_where`, so the grid, prev/next nav, filtered `/export-csv`, `/api/gallery-images` (all pickers), saved views and the MCP `search_catalog` got it in one change. |
| ~~medium~~ | `sweep-bcd` | feature | 90min | ~~Power-user — paidCredit is fetched on every generation and never persisted anywhere~~ **SHIPPED 2026-07-23** (branch `paid-credit-persist`) — `paid_credit` catalog column, captured at every site that sees it + historical recovery via `--backfill-full-meta --with-credit`; see the Tier 6 "Blocking-first" entry. |
| ~~medium~~ | `sweep-bcd` | doc-lie | 5min | ~~The kaisuukenId NOTE in build_chat_edit_parameters is contradicted by the same function 19 lines later~~ **FIXED 2026-07-22**: NOTE now describes the real opt-in behavior (kaisuuken_id defaults to "" and is only attached when the caller passes one), matching every sibling build_*_parameters builder. |
| medium | `sweep-bcd` | feature | 300min | Curator #1 — no Triage Deck; the lightbox keyboard handler has no rate/collect/delete keys |
| medium | `sweep-bcd` | feature | 240min | Loom D#2 — no per-shot takes; a re-roll silently overwrites resultMid |
| medium | `sweep-bcd` | half-migrated | 120min | Curator #2 — saved views are stored server-side but the Picker and Loom cannot use one |
| medium | `sweep-bcd` | feature | 150min | Loom D#5 — no project spend ledger or cost-to-finish; only a per-batch pre-flight estimate |
| medium | `sweep-bcd` | feature | 240min | Loom D#9 — no editor handoff export; the shot's transition/audio/note fields still do nothing downstream |
| medium | `sweep-bcd` | feature | 150min | Curator #8 — /api/collections is read-only; nothing can rename, merge or delete a collection |
| ~~medium~~ | `tests-that-dont-bite` | test-gap | 20min | ~~test_service_worker_revalidates_thumbnails' thumb_branch slice is `');\n'` and the `or` fallback makes the assertion unconditional~~ **FIXED 2026-07-22**: sliced from a marker unique to the thumb branch (`const n=fetch(e.request,{cache:'no-cache'})`) instead of a brace-count split; dropped the `or "c.put" in sw` fallback. Verified by temporarily removing the thumb branch's `c.put` call and confirming the test fails. |
| ~~medium~~ | `tests-that-dont-bite` | test-gap | 5min | ~~test_loom_notify's documented "Bite: delete the lv-overrideflash line and this fails" does not fail — a // comment satisfies it~~ **FIXED 2026-07-22**: tightened to the real rendered element (`<div className="lv-overrideflash">`), which the nearby comment's prose can't match. Verified by temporarily deleting the real render line and confirming the "Bite" claim is now literally true. |
| ~~medium~~ | `tests-that-dont-bite` | test-gap | 5min | ~~test_deep_focus_has_a_prompt_field_writing_the_base_prompt: "c.prompt" is satisfied by a comment and by c.promptOverride~~ **FIXED 2026-07-22**: tightened to the exact value-binding (`value={c.prompt `), which neither `c.promptOverride` nor the nearby comment's prose can match. Verified by temporarily rebinding the textarea to `c.promptOverrideText` and confirming the test fails. |
| ~~medium~~ | `tests-that-dont-bite` | test-gap | 5min | ~~test_subpage_headers_carry_anim_class is a complete no-op — its only assertion is satisfied by the shared stylesheet~~ **FIXED 2026-07-22**: tightened to the rendered mark span's own class attribute (`class="mark anim-classic"`), which the shared stylesheet's `.mark:not(.anim-classic)` selector text can't match. Verified by temporarily stripping the anim class from /health's mark and confirming the test fails. |
| ~~medium~~ | `tests-that-dont-bite` | test-gap | 10min | ~~test_branding_save_and_render's anim-eclipse / mk-tile assertions are true on a default page with neither applied~~ **FIXED 2026-07-22**: same fix as the row above — asserts the mark span's exact class (`class="mark anim-eclipse mk-tile"`) instead of two bare substrings. Verified by temporarily dropping the `mk-tile` class and confirming the test fails. |
| ~~medium~~ | `tests-that-dont-bite` | test-gap | 10min | ~~test_portrait_mobile_pass's `or` makes the mobile full-width drawer check land on the DESKTOP 600px rule~~ **FIXED 2026-07-22**: isolates the `@media (max-width: 480px)` block (bounded by the next media query) before checking for the real full-width rule inside it, dropping the fallback that matched the desktop `#gen-drawer.wide{width:600px;}` rule. Verified by temporarily removing the mobile rule and confirming the test fails. |
| ~~medium~~ | `tests-that-dont-bite` | test-gap | 5min | ~~test_video_v40_full_cost_warning's `"2.5" in src` is satisfied by `font-size:12.5px` in the drawer's stylesheet~~ **FIXED 2026-07-22**: asserts the exact warning text (`V4.0 full — ~2.5× Lite`) instead of a bare `"2.5"`. Verified by temporarily changing the real warning text and confirming the test fails. |
| ~~medium~~ | `unfiled-workflow-findings` | defect | 10min | ~~The stray root `HEAD` file is written by the test suite — the dashboard's "if it comes back a third time, catch what writes it" mystery, solved and reproduced~~ **FIXED 2026-07-23** — see O10 (Tier 3, above) for the root-cause fix (commit `1ba9505`); both `tests/test_panel.py` `FakeProc` classes now guard on `argv[0] == "ffmpeg"`. |
| ~~medium~~ | `unfiled-workflow-findings` | defect | 20min | ~~clamp_edit_config() is never called on the CLI edit path — only the web calls it, so the CLI sends resolution/quality a model rejects~~ **FIXED 2026-07-22** — same commit as the `orphaned-surfaces` duplicate above (Package 3). |
| ~~medium~~ | `unfiled-workflow-findings` | doc-lie | 2min | ~~STATE.md's "public surface" correction is half-applied — line 350 now says four, line 361 still says three~~ **ALREADY CORRECT 2026-07-22**: verified — both places already say "four", matching `PUBLIC_EXPECTED_STATUS`'s real 4 endpoints (login, logout, branding, manifest). Stale finding, no edit needed. |
| ~~medium~~ | `unfiled-workflow-findings` | security | 40min | ~~/panel hands every account username and the host library path to any LOGIN-tier caller~~ **FULLY RESOLVED 2026-07-22**: host path withheld from non-local callers (S2, 2026-07-21); username visibility at LOGIN-tier is a deliberate, accepted design choice, not a bug; the real write-access gap it glosses over (any LOGIN caller could add/remove accounts) was fixed the same night. Nothing left open here. |
| ~~medium~~ | `unfiled-workflow-findings` | security | 15min | ~~Cache Storage survives sign-out, and wiki/Trust-and-Safety.md still promises the opposite~~ **FIXED 2026-07-22** — see S4 above. |
| ~~medium~~ | `unfiled-workflow-findings` | test-gap | 15min | ~~PWA-on-LAN has never been verified, and three user-facing docs promise it~~ **ANSWERED 2026-07-22** — see D-9, duplicate of the `state-owner-defects` row above. |
| ~~medium~~ | `unfiled-workflow-findings` | security | 90min | ~~str(e) redaction re-spin still open — and STATE.md's counts are exactly right~~ **FIXED 2026-07-22** — see S3 above. |
| low | `unfiled-workflow-findings` | security | 20min | The str(e) redaction (S3) only recognizes 5 known directory-path strings, not a bare OS-username TOKEN sitting outside any of them (e.g. a username-prefixed filename with no full path around it). No live call site found that would trigger this today — defense-in-depth gap, not a demonstrated leak. Surfaced in adversarial review, 2026-07-22. |
| low | `unfiled-workflow-findings` | security | 15min | The str(e) redaction (S3)'s `.replace()` is case-sensitive while Windows paths are not — a differently-cased path from a third-party library or normalized OS string would silently fail to match and ship unredacted. No concrete trigger found among the current 37 wrapped sites. Surfaced in adversarial review, 2026-07-22. |
| medium | `wiki-truth` | doc-lie | 25min | ~~Backing-Up.md says the ↑ Import button "only appears for a local session" — it is rendered to every signed-in session and 403s after the upload finishes~~ **FIXED 2026-07-21** |
| ~~medium~~ | `wiki-truth` | doc-lie | 30min | ~~Two wiki pages promise PWA install on a launch path where browsers refuse to register a service worker~~ **ALREADY CORRECT 2026-07-22**: checked wiki/Gallery.md and wiki/FAQ.md (and their full git history) — both always paired the PWA claim with `--https`, never promised it on plain HTTP. Stale finding, no edit needed. |
| ~~medium~~ | `wiki-truth` | doc-lie | 20min | ~~How-It-Works.md says `--watch` and `--claims` are CLI-only~~ **FIXED 2026-07-23** (web equivalents named). |
| medium | `wiki-truth` | doc-lie | 10min | ~~Generating.md's "unsupported Mode is harmless, it auto-falls-back" sits under the web-drawer section, where no fallback exists~~ **FIXED 2026-07-21** |
| medium | `wiki-truth` | doc-lie | 10min | ~~Control-Panel.md promises a live job log to LAN sessions; as of today's security fix they get a placeholder line instead~~ **FIXED 2026-07-21** |
| ~~medium~~ | `unfiled-workflow-findings` | orphaned | 90min | ~~The Edit tab's Fix sub-tab (hand/face box-fixer) has no price check of any kind, not even a `window.confirm()`.~~ **PARTIALLY FIXED 2026-07-23**: confirmed via `private/GENERATOR_SURFACE.md`'s reverse-engineered parameter schema that `POST /v2/task/fixer` is a genuinely separate endpoint from the `createGenerationTask`/`check`/`task-price` family — real backend pricing support isn't something our client code can add, this really is blocked on PixAI's own API as originally suspected. Added the `window.confirm()` fail-closed guardrail instead (this app's own established pattern for a spend surface that can't be priced), so the Fix tab no longer submits with zero warning of any kind, matching every other spend-capable surface's minimum bar. |
| ~~low~~ | `unfiled-workflow-findings` | orphaned | 30min | ~~`--full-meta`/`--backfill-full-meta` don't actually get the ad-hoc-fallback resilience `_task_detail_query`'s own docstring claims they do. That fallback's only real caller is `collect_generation` (task-id recovery); both `--full-meta` and `--backfill-full-meta` call `task_detail_gql` directly, raw, with no fallback.~~ **RESOLVED 2026-07-23** (commit `e36976d`): the docstring was the lie, and it now tells the truth — it states plainly that the fallback is NOT shared by `--full-meta`/`--backfill-full-meta`, names `collect_generation` as the only real caller, and records the rewiring question's answer inline: "would be a real behavior change — not done here." Deliberately-not-rewired is the decided state (low stakes: `TASK_DETAIL_HASH` ships baked-in and is essentially never empty), not an open question anymore. |
| ~~low~~ | `unfiled-workflow-findings` | orphaned | 20min | ~~The `is_local=True` hardcoded literal that gates the header's "↑ Import" button display (`pixai_gallery.py` template, ~3973-3979) has no real security hole — `/api/import-local` is correctly re-checked server-side as LOCALHOST-tier (`_is_local_request()`) — but the nearby comment justifying the hardcode only enumerates "Generate/Loom/Panel" (all genuinely LOGIN-tier) and doesn't account for Import, which is LOCALHOST-tier.~~ **FIXED 2026-07-23** (commit `e36976d`, the comment-correction option this row itself offered): the `is_local` comment (now `pixai_gallery.py:~9031-9037`) explicitly documents Import as "the one documented exception" — rendered under the blanket display flag, re-checked by `/api/import-local`'s stricter `_is_local_request()`, so a signed-in non-local LAN session sees the button but gets a 403. The LAN-sees-a-403ing-button UX itself is unchanged and accepted (documented behavior now, not an unexplained inconsistency). |
| ~~low~~ | `auth-tier-drift` | security | 25min | ~~/api/watch/status returns an unredacted `last_error` to LAN callers, and it is stored-then-served so a jsonify-site redaction pass will miss it~~ **FIXED 2026-07-22** — see S3/Package 5: both `_watch_status["last_error"]` write sites wrapped in `_redact_host_paths`, exactly the "stored-then-served" case that sweep was built to catch. |
| ~~low~~ | `auth-tier-drift` | doc-lie | 5min | ~~docs/architecture.md says /api/branding POST is localhost-only; it is LOGIN — the localhost route is the separate /api/branding/shortcut~~ **FIXED 2026-07-22** — same commit as the `fresh-clone`/P2 duplicates above. |
| ~~low~~ | `auth-tier-drift` | doc-lie | 5min | ~~The Your Art panel blames a nonexistent localhost gate for a failed view fetch~~ **FIXED 2026-07-23** (commit `4d07548`): reworded to "live views load lazily, from any signed-in device" (`pixai_gallery.py:5732`). |
| ~~low~~ | `cli-surface` | orphaned | 40min | ~~34 live CLI flags are documented nowhere, including --faststart-videos, the fix for the known iOS video-playback bug~~ **FIXED 2026-07-23**: the real count was 30 once the since-documented ones were netted out; all 30 verified against their argparse defs + consuming code, then documented in `docs/architecture.md`'s new "CLI flags" section (grouped, with the non-obvious gotchas: `--params-json` hard-override, edit-param clamping, seconds-vs-count units, partner-flag requirements) and in the user-facing `wiki/Backing-Up.md` (download tuning / convert / live watch / catalog repair) + `wiki/Generating.md` (edit tuning / video tuning / shared create flags / `--contests`). `--audio-language` and `--poll-timeout` also had NO `--help` text at all — both added, so `--help` is now complete. |
| ~~low~~ | `cli-surface` | doc-lie | 10min | ~~wiki/Generating.md's copy-pasteable --suggest-prompt example now returns a 500~~ **FIXED 2026-07-23**: added a caveat that the specific example id is just a real one from this repo's history, not a guarantee — PixAI's endpoint can fail on old-enough media even for a genuine image, swap in any recent media_id from your own catalog. |
| ~~low~~ | `fail-open` | defect | 10min | ~~/api/skin and /api/achievements?mark=1 return HTTP 200 success when the persist actually failed~~ **FIXED 2026-07-23** — see B18 above: both now check `save_ach_state()`'s return value and report the failure + actually-active value instead. |
| ~~low~~ | `fail-open` | defect | 5min | ~~/api/loom/delete answers {"ok": true} when the file delete failed — or when nothing was deleted at all~~ **FIXED 2026-07-23**: a real `OSError` now returns `{"ok": false, "error": ...}` (500); `FileNotFoundError` (nothing to delete) still returns `{"ok": true}` by design, matching `loom_set`. |
| ~~low~~ | `fail-open` | defect | 10min | ~~savePreset() shows nothing at all when the server rejects a saved view~~ **FIXED 2026-07-23**: now shows a `Toast` error on a server rejection (`pixai_gallery.py:4479-4489`). |
| ~~low~~ | `fail-open` | defect | 15min | ~~run_sync_artworks silently truncates on a mid-pagination failure and prints a complete-looking total~~ **FIXED 2026-07-23** (commit `6416d2b`, see B15 above). |
| ~~low~~ | `fresh-clone` | orphaned | 10min | ~~static/_mockup_login_panel.html is cited six times as the "locked design source" but .gitignore guarantees it can never be in a clone~~ **FIXED 2026-07-23** — resolved the stronger way per owner direction: internal dev/creative-process material doesn't belong in the public repo at all. All six mockup citations removed (4 in `pixai_gallery.py` — one of which shipped inside the served /login CSS — plus 2 in `tests/test_web_auth.py`), and the same pass stripped every quoted owner directive, "owner directive <date>" attribution, Figma/Figma Make provenance note, and session-artifact id from code comments across `pixai_gallery.py`, `pixai_gallery_backup.py`, `static/mg-notify.js`, `static/mg-generate-drawer.js`, `loom/master-storyboard.jsx`, and seven test files — each rewritten to state the technical rule on its own terms. Product strings (achievement roasts etc.) untouched by design. |
| ~~low~~ | `fresh-clone` | doc-lie | 5min | ~~README carries three headings whose entire body is a caption for a screenshot that was deleted and never replaced~~ **FIXED 2026-07-23** (commit `d017198`): the three orphaned subheadings removed from `README.md`; no "screenshot" text remains anywhere in the file. |
| ~~low~~ | `fresh-clone` | doc-lie | 20min | ~~README's headline "make it yours: pick your header mark" feature ships zero assets, so it is empty on every clone~~ **FIXED 2026-07-23** (commit `d017198`): reworded to "drop your own header marks into `pixai_backup/branding/marks/` and pick one + its animation ... (none ship — bring your own art)" — no longer implies bundled assets exist. |
| ~~low~~ | `fresh-clone` | defect | 15min | ~~The "python 3.8+" badge is unverified — CI only tests 3.11, and two committed files already need 3.9+~~ **FIXED 2026-07-23** (commit `d017198`): badge corrected to `python-3.9%2B` in `README.md`; spot-confirmed real PEP 585 generic syntax (`list[str]`) in `moonglade_mcp.py:125`, which requires 3.9+. The commit message's fuller "4 test files" tally was not independently re-verified. CI still only tests 3.11, so 3.9 is asserted but not actually exercised in CI — a narrower residual, not reopened here. |
| ~~low~~ | `fresh-clone` | doc-lie | 2min | ~~CONTRIBUTING says "five modules", the doc it links to in the same sentence says four~~ **FIXED 2026-07-23**: `CONTRIBUTING.md` now says "four modules", matching `docs/architecture.md`'s "four Python modules". |
| ~~low~~ | `fresh-clone` | security | 10min | ~~No SECURITY.md, so GitHub surfaces no vulnerability-reporting path for a public repo that gates credit-spending routes~~ **FIXED 2026-07-23** (commit `d017198`): `SECURITY.md` added at repo root, mirroring `CONTRIBUTING.md`'s existing email-based reporting convention; correctly does NOT claim GitHub's private-vulnerability-reporting feature is live (re-confirmed still off via `gh api repos/Nelnamara/moonglade-athenaeum/private-vulnerability-reporting` → `{"enabled":false}`). |
| low | `frozen-roadmap` | test-gap | 20min | hand-fix / face-fix workflow names are still self-flagged "unverified until fired" with no verification record and no tracker entry |
| ~~low~~ | `frozen-roadmap` | feature | 30min | ~~§4's `mascots.json` asset→role registry was never scaffolded and is tracked nowhere~~ **RESOLVED 2026-07-23 by NOT building it** — confirmed `docs/ART.md`'s asset table (§ rows, e.g. row 11 "Toast mascot") already IS the asset-to-role registry this would have duplicated, including the non-achievement roles that were the only argument for a separate file. Matches this doc's own §3 "Untriaged" recommendation ("`mascots.json` should probably just be dropped"). |
| low | `frozen-roadmap` | orphaned | 2min | Branch hygiene rotated: the three named branches are gone, but a new fully-merged branch sits on origin |
| ~~low~~ | `frozen-roadmap` | doc-lie | 5min | ~~`build_shot_video_params`'s docstring cites a STATE.md section that no longer documents the R2V negative-prompt gap~~ **FIXED 2026-07-23** (commit `a4a3de7`): docstring (`pixai_gallery_backup.py:3761-3773`) now states the R2V/V2V negative-prompt API gap directly, with no STATE.md citation. |
| ~~low~~ | `gui-removal-residue` | doc-lie | 5min | ~~Image detail page tooltip tells you to paste the media id "into the GUI Video/Edit tab"~~ **FIXED 2026-07-22** — same commit as the `orphaned-surfaces` duplicate above (now "into the Edit tab or the Loom"). |
| ~~low~~ | `gui-removal-residue` | doc-lie | 20min | ~~Thirteen comments and docstrings still describe the desktop GUI as a live surface~~ **FIXED 2026-07-22** — same commit as the `orphaned-surfaces` duplicate above; a case-sensitive `\bGUI\b` sweep of pixai_gallery.py now returns zero matches. |
| low | `gui-removal-residue` | orphaned | 10min | pixai_gallery.py's --open-browser flag has zero callers (re-verified still true — no launcher script in the repo passes it) and ~~its help text credits a launcher that doesn't use it~~ **FIXED 2026-07-23**: reworded to describe manual terminal-launch use, with no false launcher credit. |
| ~~low~~ | `half-migrated` | half-migrated | 10min | ~~mg-notify's host-neutral font fix covered three roots but not the achievement celebration or the Folio of Honors subtree~~ **FIXED 2026-07-23** (commit `18541cb`): `.ach-m2` (the achievement celebration toast) and `#ach-modal` (the Folio of Honors panel) now both state `font-family:system-ui,sans-serif` explicitly (`static/mg-notify.js:54,291`), same fix as the original three roots. `tests/test_loom_notify.py::test_notify_components_do_not_inherit_their_font_from_the_host_page` was extended to check all five roots. |
| low | `half-migrated` | orphaned | 15min | Three of mg-gallery-picker's four documented parity flags have zero callers outside the dev harness |
| low | `half-migrated` | half-migrated | 30min | The picker drift now runs the other way: the shared component has a thumbnail-size slider the gallery's own picker never got |
| low | `half-migrated` | orphaned | 10min | mg-cost-badge's documented `compact` mode has no caller anywhere |
| ~~low~~ | `half-migrated` | doc-lie | 5min | ~~mg-generate-drawer documents a `tab` attribute it never reads~~ **FIXED 2026-07-23** (commit `2b891df`): usage comment corrected to `<mg-generate-drawer></mg-generate-drawer>`, and now documents the one attribute the element actually reads (`data-loom-ctx`); the dev harness's stale `tab="video"` was removed too. |
| ~~low~~ | `half-migrated` | orphaned | 20min | ~~Orphaned CSS left behind by the gallery Video-tab swap: .vp-chip and .gen-ce have no producers~~ **FIXED 2026-07-23** (commit `4d07548`): both dead rules deleted; zero remaining matches for either selector. |
| ~~low~~ | `half-migrated` | doc-lie | 5min | ~~Two components cite docs/SUITE_ARCHITECTURE_AUDIT.md, which was archived and no longer exists at that path~~ **FIXED 2026-07-23** (commit `d017198`): both `static/mg-model-picker.js` and `static/mg-gallery-picker.js` now cite `docs/archive/SUITE_ARCHITECTURE_AUDIT_2026-07-13.md, archived`. |
| ~~low~~ | `half-migrated` | doc-lie | 30min | ~~docs/architecture.md never mentions the five shared web components or the static/ directory at all~~ **FIXED 2026-07-23** (commit `d017198`): new "Shared web components (`static/`)" section (`docs/architecture.md:234-254`) documents all 5 `mg-*.js` files + `picker-core.js`. |
| ~~low~~ | `invariants` | doc-lie | 20min | ~~INVARIANT 2's "checked before any network call" is not true under --full-meta / --sync~~ **FIXED 2026-07-23** (commit `e36976d`, see P7 above). |
| ~~low~~ | `invariants` | doc-lie | 10min | ~~docs/architecture.md still says thumbnails are served immutable for a year — the code changed to max-age=300 one day after the doc reconciliation and the doc was not updated~~ **FIXED 2026-07-23** (commit `d017198`): corrected to `(short-lived cache)` / `public, max-age=300` + ETag, matching `pixai_gallery.py:9258`. |
| ~~low~~ | `invariants` | doc-lie | 10min | ~~docs/architecture.md's module table describes already_downloaded() with the wrong glob and calls it "the resume check" when run_download no longer calls it~~ **FIXED 2026-07-23** (commit `d017198`): `docs/architecture.md:33` now names the real `find_files_for_media_id` matcher and correctly attributes the only call site to `run_sync_artworks`, not `run_download`. |
| ~~low~~ | `invariants` | test-gap | 30min | ~~INVARIANT 5's byte-safe clause is the one part of --organize with no test — nothing exercises the identical-dupes-dropped or differing-kept-side-by-side branch~~ **FIXED 2026-07-23** (commit `2040fa9`; Tier 5 #4's narrative copy was struck same day, this appendix twin was missed): `tests/test_filesystem.py::test_organize_drops_byte_identical_duplicate` + the differing-content-kept sibling now cover both branches. |
| ~~low~~ | `invariants` | half-migrated | 15min | ~~INVARIANT 1's "single source" media_id_of() is re-implemented inline in two other modules~~ **CORRECTED 2026-07-23** (see P7 above) — only one other module actually does (`pixai_similar.py`'s `scan_dir`); doc no longer claims "single source" either way. |
| low | `invariants` | defect | 15min | --rebuild-similar embeds quarantined and purged images: scan_dir excludes only "gallery", not _duplicates/ or _deleted/ |
| ~~low~~ | `orphaned-surfaces` | orphaned | 10min | ~~The PySide6 removal left three GUI-only wrapper functions behind with zero callers~~ **FIXED 2026-07-23** (commit `a4a3de7`): `run_verify_dupes`, `run_audit`, `run_dedup` deleted; the real `cmd_*` logic they wrapped is untouched. Same commit as O6 above and the `gui-removal-residue` appendix row (~line 379). |
| ~~low~~ | `orphaned-surfaces` | orphaned | 25min | ~~/contact-sheet's ?collection=, ?cols= and ?captions= parameters have no UI entry point~~ **FIXED 2026-07-23, the `?collection=` half** (commit `e38c784`, see O5 above): a "Contact sheet" button now sits beside the Download-collection ZIP button. `?cols=`/`?captions=` remain URL-only knobs with no UI — deliberate power-user parameters, not worth chrome. |
| ~~low~~ | `orphaned-surfaces` | doc-lie | 15min | ~~STATE.md's "What's still CLI-only ... Nothing" is not true -- --faststart-videos and --convert-existing have no web surface~~ **FIXED 2026-07-22** — same commit as the `gui-removal-residue` duplicate above. |
| low | `orphaned-surfaces` | orphaned | 20min | <mg-cost-badge>'s mg-cost event and compact attribute have no consumer outside the dev harness |
| ~~low~~ | `orphaned-surfaces` | orphaned | 5min | ~~is_lora_type() is defined and referenced by nothing anywhere in the repo~~ **FIXED 2026-07-23** — same commit as the row above (`a4a3de7`): the function is deleted; confirmed zero remaining references anywhere in the repo. |
| low | `schema-config-drift` | orphaned | 30min | batches/ can no longer be created by any code path, but the reader, the index and the bucket-priority table still target it |
| ~~low~~ | `schema-config-drift` | orphaned | 5min | ~~ARTWORK_DETAIL_HASH is read from config and given a baked default, then never used anywhere~~ **FIXED 2026-07-23** (commit `c587153`; the Tier 3 "Also:" narrative copy was struck same day, this appendix twin was missed): the dead config read and its `config.example.json` entry are both deleted — zero remaining references. |
| low | `schema-config-drift` | doc-lie | 5min | AUTH_EPOCH_SEQ is written into config.json by the app but appears in no example or doc |
| ~~low~~ | `schema-config-drift` | doc-lie | 3min | ~~The view-presets filename comment claims dots are percent-escaped; quote(safe="") does not escape dots~~ **FIXED 2026-07-23** (commit `4d07548`): `_view_presets_path()`'s comment (`pixai_gallery.py:10762-10770`) now correctly says letters/digits/`_`/`.`/`-`/`~` are RFC-3986-unreserved and always left literal by `quote()`, and explains why that's harmless here (one filename component, never a path). |
| low | `schema-config-drift` | doc-lie | 3min | The route-tier table still carries the pre-split rationale that v2.2.0 explicitly repudiated |
| ~~low~~ | `schema-config-drift` | defect | 5min | ~~.gitignore only generalizes jobs.jsonl beyond the default out_dir name, so a custom --out leaves catalog.db and images unignored~~ **FIXED 2026-07-23** (commit `1ba9505`): `.gitignore` gained unanchored patterns for `catalog.db`, `catalog.csv`, `gallery/`, `images/`, `videos/`, `imported/`, `_duplicates/` and the `*_report.csv`/manifest files, matching the trick `jobs.jsonl` already used. Same commit root-caused the stray root `HEAD` file (see the `unfiled-workflow-findings` row above). |
| low | `state-next-epics` | feature | 0min | Loom opt-in larger text/button scale: still unbuilt — the only toggle in the shell is Cast-tab content density |
| low | `state-next-epics` | feature | 0min | Gallery adoption of <mg-model-picker>: still open — the gallery loads #model-flyout, the component is Loom-only |
| low | `state-next-epics` | feature | 0min | Epic A (The Foundry): still zero code — the "nothing Blender-related exists in the repo yet" claim is exactly right |
| low | `state-next-epics` | feature | 0min | Epic B (The Provider Deck): still open, and the four functions it names as the existing seam all exist |
| low | `state-next-epics` | feature | 0min | Mio.2: still zero code, and the "do not capture cookies" guard is still unviolated |
| low | `state-next-epics` | feature | 0min | Epic C (Publish & Community): still open — no publish path exists in tracked code, and --sync-artworks is still read-only |
| low | `state-next-epics` | feature | 0min | BlurHash placeholders: every clause of the bullet checks out — column populated, published-only, no decoder |
| low | `state-next-epics` | feature | 0min | Gallery QoL backlog: all six items re-verified absent, and the "filtered export now ships" sub-claim is true and reachable |
| low | `state-next-epics` | feature | 0min | WoW-style casting-bar frame art: still banked — border-image is used only by the unlock toast's tier frames |
| ~~low~~ | `state-owner-defects` | defect | 0min | ~~Native <select> styling split confirmed: three selectors get appearance:none, three keep the OS arrow~~ **FIXED 2026-07-23** (commit `4d07548`): `.pick-filters select` (`pixai_gallery.py:3624`) joined `.filters select`/`#preset-select`/`select.p-sel`'s existing custom-arrow rule, and a new `select.gen-sel` rule (`:5130-5132`) gives the Generate drawer's selects the same treatment — all native selects now match. |
| ~~low~~ | `state-owner-defects` | defect | 15min | ~~Activity chip dead zone is permanent on the gallery, not intermittent — the FAB is shown whenever the tray is closed~~ **FIXED 2026-07-23** (commit `4d07548`): `.grid { padding-bottom: 64px; }` added (`pixai_gallery.py:3691`), giving `#jobs-fab` real clearance — the FAB itself still shows persistently by design, only the dead zone under it is gone. |
| low | `state-owner-defects` | defect | 0min | Loom corner-FAB stacking residual confirmed structurally: .lv-df-veil really does render inside .lv-overlay |
| low | `state-owner-defects` | decision | 0min | **Already decided, not an open item — recorded so it isn't relitigated:** `/sw.js` stays LOGIN-gated on purpose (owner call 2026-07-21, taken alongside making `/manifest.webmanifest` public), so the service worker never registers while sitting on the login page. Making it public would arm a fetch handler + origin-scoped Cache Storage in browsers that never authenticate, over routes they can't read anyway. Cost is one navigation: registration fails at the 302 and `.catch()` swallows it, so the thumbnail cache arms on the first authorized navigation instead. Mechanism note: a worker *script* fetch uses redirect mode `"error"`, so it never generates the `/login?next=` hit that justified the public manifest — do not "fix" this by allowlisting `/sw.js` |
| low | `state-owner-defects` | decision | 0min | `index()` passes `is_local=True` as a literal, so the header's "read-only LAN view" branch is unreachable and the variable name is a misnomer for "authorized" — deliberate and documented at the call site (the front-door hook guarantees an authorized request reaches that line), so a naming/clarity wart, not a bug. Renaming it touches auth-adjacent template logic and wants an owner nod rather than a drive-by |
| low | `state-owner-defects` | decision | 0min | Epic-tier frame art really is a one-key change; per-tile ornate frames really are unbuilt |
| low | `state-owner-defects` | decision | 0min | Toast tier colours still run the original scheme, exactly as the open call states |
| low | `state-owner-defects` | decision | 0min | Generation Flags is confirmed at zero code, and the numpy claim is right — it is imported but not a declared dependency |
| low | `state-owner-defects` | decision | 5min | File logging confirmed entirely absent — neither module even imports logging |
| low | `state-owner-defects` | decision | 0min | Gallery search-bar redesign confirmed unstarted; the only filters chrome is the mobile bottom sheet |
| low | `state-owner-defects` | decision | 10min | SFX loader confirmed shipped and fail-soft; the drop-in path is documented but has no in-app affordance |
| low | `state-owner-defects` | decision | 0min | ~~Trophy Hall rename confirmed still open — UI reads "Trophy Hall" everywhere~~ **FIXED 2026-07-22**: renamed The Folio of Honors, owner's pick off the shortlist |
| low | `sweep-bcd` | feature | 90min | Curator #6 — the cloud-delete confirm warns about siblings in prose but never shows them |
| low | `sweep-bcd` | feature | 300min | Curator #4 — no near-duplicate clustering; the CLIP sidecar only answers 1→k |
| low | `sweep-bcd` | feature | 45min | Curator #9 — no archive integrity job; /health covers only one of the four checks and is page-load-only |
| low | `sweep-bcd` | feature | 120min | Curator #5 — no stack-by-batch grid toggle |
| low | `sweep-bcd` | feature | 150min | Loom D#7 — no re-anchor warnings on the reel |
| low | `sweep-bcd` | defect | 30min | Loom D#8 — Play sequence is hard-muted while the exported mp4 carries audio |
| low | `sweep-bcd` | feature | 90min | Power-user — no Remix from the lightbox; the three lightbox actions send the image, never the recipe |
| low | `sweep-bcd` | feature | 90min | Power-user — no model/LoRA favorites or recents in the picker |
| low | `sweep-bcd` | feature | 180min | Power-user — Contest workbench is half-built: deadlines shipped, shortlist staging did not |
| low | `sweep-bcd` | feature | 480min | Power-user — no prompt-matrix queue runs |
| low | `sweep-bcd` | feature | 120min | Power-user — no card-utilization digest; the balance chip covers expiry but not per-model coverage or expired-unused |
| low | `sweep-bcd` | feature | 240min | Power-user — no metadata recovery for hand-made folders |
| ~~low~~ | `sweep-bcd` | doc-lie | 5min | ~~The --watch STATUS paragraph is superseded by the comment six lines below it~~ **FIXED 2026-07-23** (commit `a4a3de7`): the STATUS comment above `run_watch` (`pixai_gallery_backup.py:5145-5148`) now describes the shipped `--watch-backup`/`_WS_DONE_STATUS` state directly instead of framing it as future work. |
| ~~low~~ | `tests-that-dont-bite` | test-gap | 5min | ~~test_no_real_newline_inside_confirm_string's real check is already FALSE today and is masked by an unrelated whole-page fallback~~ **FIXED 2026-07-23** (commit `119326b`): tightened to pull each quoted JS literal out of the matched `confirm()` span and check each ONE for a real newline, instead of checking newline-freedom across the whole span and papering over the resulting false positive with `"\n\n" in html`. |
| ~~low~~ | `tests-that-dont-bite` | doc-lie | 5min | ~~test_cost_badge_ships_with_every_price_surface claims "every surface" but the Loom's two cost surfaces are not covered and are not badges~~ **FIXED 2026-07-23** (commit `119326b`): docstring narrowed to its real scope (the `<mg-cost-badge>`/script pairing) and now names the two genuinely-uncovered non-badge cost surfaces instead of claiming "every surface." |
| ~~low~~ | `tests-that-dont-bite` | test-gap | 5min | ~~test_organize_and_dedup_unaffected never inspects cmd_organize~~ **FIXED 2026-07-23** — same commit as the row above (`119326b`): added the missing `inspect.getsource(core.cmd_organize)` check alongside the existing `cmd_dedup` one. |
| ~~low~~ | `unfiled-workflow-findings` | orphaned | 10min | ~~`--variant` is a dead CLI flag that the module's own usage banner still advertises as a real command~~ **FIXED 2026-07-22** — see D-5: flag and its docstring example both deleted. |
| ~~low~~ | `wiki-truth` | doc-lie | 10min | ~~Setup.md lists `cryptography` in the requirements-install table, but it is not in requirements.txt~~ **FIXED 2026-07-23** (commit `d017198`): the table entry now explicitly says "(**not** in `requirements.txt` — `pip install cryptography` separately)". |
| ~~low~~ | `wiki-truth` | decision | 15min | ~~Home.md and FAQ.md call this a "personal-use tool" while the code's own security reasoning says the opposite~~ **FIXED 2026-07-23**: `Home.md` reworded to "runs against your own PixAI account" with an explicit note that it's a public, multi-account-capable tool (linking `Trust-and-Safety`); `FAQ.md`'s "personal-use tool" → "independently-run client... anyone can self-host it." |

**Owner field-test findings, 2026-07-23 (live Loom/gen-panel session after v2.3.0):**

| sev | lens | category | est | finding |
|---|---|---|---:|---|
| ~~high~~ | `owner-2026-07-23` | defect | ? | ~~**Videos corrupt on collect.** Two completed videos are fine on PixAI's side but the catalog copy truncates mid-play (one stops at 11s, one at 8s). Suspects to rule out: a partial download promoted to a final filename (the B1 zero-byte fix guards empty bodies, not mid-stream cuts — is Content-Length verified?), or the live-mirror `--watch-backup` collecting the instant the task completes, before PixAI finishes finalizing the mp4 server-side.~~ **FIXED 2026-07-23** (commits `63b70ca`, `842516a`): both suspects were wrong — byte forensics against the real corrupted files traced it to TWO collectors in the gallery process (the always-on live-mirror watcher AND a LAN client's `/api/task-status` poll) running `collect_generation` for the same task seconds apart; `download()` correctly skipped the second copy, but both passes ran `video_faststart`, whose remux temp name was DETERMINISTIC — the two concurrent ffmpeg runs interleaved writes into the same temp file and the survivor carried stale pre-shift windows exactly one moov-size offset out of place (full-length mp4, stops playing mid-way). Fixed in three layers, each verified fail-first: unique-per-invocation remux temp (the cross-process guard — `--watch-backup` is a separate process), single-flight collect per task id across watcher/task-status/import-task, and `download()` now verifying bytes-written against `Content-Length` (the "is Content-Length verified?" side-question was real — it wasn't, and a mid-stream cut would have been promoted; closed as part of this pass even though it was not this incident's cause). |
| high | `owner-2026-07-23` | defect | ? | ~~**GitHub issue #3 (P1, owner-filed, video attached):** the Loom's left-side asset picker does not import already-rendered videos into the tray when selected — they should land in the tray for timeline/scene-reference use. Filed against v2.2.0 `74bcc6e`; re-verify against current before fixing.~~ **FIXED 2026-07-23** (commit `06b1591`, branch `loom-issue3-lora-toggle`): the picker now adds a selected video to Cast & Assets like an image does, poster/thumbnail included. Fail-first tested (`loom/test/loom-picker-video-import.test.js`). |
| medium | `owner-2026-07-23` | defect | ? | **Loom shot-card reference sending bugs out past 2 images — CONFIRMED and precisely reproduced from the owner's screen recording ("Loom issue Recording", shot A-01, Multi-Reference/R2V mode).** Not a "past 2 images" cap — the corruption starts the moment the "Pick from your gallery" reference picker is opened, before anything is even selected. Frame-by-frame: at the point the picker opens, the shot's auto-composed prompt correctly reads `"...her lover Greg @image4 lying face down..."` (Greg's own cast tag). By the time the picker is closed with nothing added, the SAME sentence has become `"...her lover@image3..."` — `@image3` is **Nelnamara's own cast tag**, not Greg's. A few seconds later, with still nothing added, the tag is gone entirely: `"...her lover Greg lying face down..."`, no reference marker at all. Immediately after, a `"override active — fields above not woven in"` banner appears — the (now-corrupted) prompt has been silently frozen as a hand-edited override, so the Camera/Lighting/Cast fields can no longer re-compose it correctly even if the owner fixes them. The Image References panel itself never advances past the original @image1/@image2 the whole time, no matter how long the picker is browsed — nothing ever successfully gets added. The owner worked around it by abandoning the add-reference attempt and generating with the original 2 refs, which did render successfully — confirming PixAI's own side is fine and this is 100% client-side prompt-composition state. Root cause not yet traced (needs a look at whatever recomposes the Multi-Reference prompt on picker-open/close in `loom/master-storyboard.jsx` / `loom-core.js`) — but the trigger and the exact corruption sequence are now fully pinned from video evidence, no further repro needed. |
| medium | `owner-2026-07-23` | defect | ? | **Status mismatch:** a generation in the Loom shows as actively rendering while the Job Tracker simultaneously lists the same task as rendered/still. One of the two surfaces is reading the wrong signal. |
| medium | `owner-2026-07-23` | half-migrated | ? | **Loom image gen is stripped bare — CONFIRMED by direct visual comparison** (owner's "Model-lora picker Recording"). Side by side in the same recording: the gallery's own Generate tab (Image mode) has Advanced (collapsible), 8 Aspect-ratio buttons, Size + custom W×H, Mode, Count, Seed, High-priority checkbox, Prompt helper checkbox — all present. The Loom's Image tab, same moment, same account: Model search, "+add LoRA", one Image-prompt textarea, "Generate reference image" button. Nothing else. Every field between those two is simply absent from the Loom's version, not hidden behind a collapsed section. |
| ~~medium~~ | `owner-2026-07-23` | feature | ? | ~~**Generate button locks until the current gen completes** in every gen panel — PixAI itself allows multiple concurrent generations. Unlock concurrent submits (respecting the existing cost-gate flow per submission).~~ **FIXED 2026-07-23** (commit `4cb1054`, branch `concurrent-generations`): all three lock mechanisms found were per-surface, not shared — the gallery's `runTask()` (Generate/Edit/Enhance/Fix) and the shared `<mg-generate-drawer>` (gallery Video tab + the Loom's Deep Focus) both disabled their Go button from submit until the task's poll reached a terminal phase; both now free the button the moment the SERVER ANSWERS the submit (accepted or rejected), and each concurrent submission gets its own appended result-strip line instead of one shared `innerHTML` a second submission would overwrite. The Loom's per-shot pipeline (`generateShot`/`pollShot`/`batchGenerate` in `master-storyboard.jsx`) needed no change — it was already keyed per shot id with a fire-and-forget poll, so different shots already rendered concurrently; `batchGenerate`'s own `todo` filter already refuses to resubmit a shot still `"wip"`. Spend gates (the Fix tab's `window.confirm`, the price-check flow) are unchanged and still run per submission. Fail-first tested: `tests/test_concurrent_generations.py`, `loom/test/mg-generate-drawer-concurrent.test.js`, `loom/test/loom-batch-generate-concurrency.test.js`. |
| low | `owner-2026-07-23` | defect | ? | ~~The Loom's LoRA picker show/hide toggle is tiny, unformatted link text — needs the app's real button/toggle chrome.~~ **FIXED 2026-07-23** (commit `d092c55`, branch `loom-issue3-lora-toggle`): restyled to the app's existing button/chip chrome, matching its neighbors. Fail-first tested (`loom/test/loom-lora-toggle-chrome.test.js`). |
| low | `owner-2026-07-23` | defect | ? | **The model/LoRA picker's hover preview looks good but obscures too much of the picker while browsing — CONFIRMED, measured from the owner's recording.** Hovering a model tile spawns a floating preview card roughly 300×350px (cover image + name + use-count/like/comment stats + a multi-line description) positioned so it covers the ENTIRE left panel (Cast & Assets / Act setup) behind it, not just a small area near the cursor. Replace with a calmer, less intrusive behavior — owner has screenshots; confirm the replacement's shape with him before building (placement rule; restyles a shipped surface). |
| low | `owner-2026-07-23` | feature | ? | **Loom workflow guide:** a diagram/wireframe walkthrough of how the Loom is meant to be used, start to finish. Deliverable, not a defect. (Noted alongside: the per-shot-card render status display is liked — keep it through any rework.) |
| — | `owner-2026-07-23` | design-pass reference (not a defect) | 0min | **Design-pass source material, owner-curated:** 8 of the screenshots in "Screenshots for Moonglade refs" (171433–171926) are PixAI's own site UI, deliberately captured by the owner as reference for the picker/generate-form redesign — NOT bug evidence. They show: PixAI's Model/LoRA browse modal (Preset/Market/Bookmark tabs, sort/filter chrome, the right-side model detail panel), and PixAI's own full mobile-width generate form (Resolution, Batch count, ControlNet, Advanced/Negative/Sampling Steps/Method/CFG/Shift/Rescale/Seed/VAE). Pull from these when building the stripped-Loom-image-gen fix above and the tooltip redesign — they're the closest thing to a pixel source of truth PixAI itself offers for both. |
| — | `owner-2026-07-23` | note | 0min | Edit Pro / Reference Pro generations are flaky, but the owner observes the same flakiness on PixAI's own site — monitor, not filed as our defect. |
| medium | `owner-2026-07-23` | feature | ~45min | **Tell the owner when he's getting the stranger treatment on his own machine.** Discovered 2026-07-23: the owner spent a day browsing via his LAN IP instead of localhost without realizing it — same physical machine, but every localhost-gated owner feature silently degraded (Panel live-log replaced by a placeholder, Import button 403s, Delete-from-PixAI hidden, install path withheld). The app can detect this exact case — request arriving on a LAN address whose source IP belongs to the server host itself — and should show a one-line dismissible hint ("browsing via the network address; owner-only features need localhost") instead of silently masking. Owner approved logging this. Note: this accident also mislabeled a chunk of his Job Tracker complaints — see the tracker row below, whose click-by-click retest should run from localhost. |
| high | `owner-2026-07-23` | defect | ? | **The Job Tracker "runs like shit and is not informative — it feels broken" (owner verdict, direct quote).** ~~Awaiting his click-by-click walkthrough~~ **LOCALHOST RETEST DONE 2026-07-23 — the LAN mask explained part of it, but real defects survive the retest; see the detailed rows below.** The naming pass stays parked regardless — that gate was "after the job tracker is actually good in his hands," and it still isn't. |
| high | `owner-2026-07-23` | defect | ? | **Confirmed by the localhost retest: a completed generation can leave its job orphaned at "running" forever, with no way for the UI to know it resolved.** Reproduced live and captured verbatim in the owner's own `jobs.jsonl`: task `2037215124834251576` logged exactly one event — `{"status":"running","type":"generate","label":"Generated"}` at `ts:1784859353` — and **never got a second event**. The generation actually finished on PixAI's side, but nothing on our end (neither the polling browser tab nor the live-mirror watcher) ever successfully called `/api/task-status` to close it out, so its only terminal-state writer (`api_task_status()`, `pixai_gallery.py:12075-12088`) never ran. The owner did a manual task-id recovery, which correctly imported the 4 media (`{"job_id":"import-34251576","status":"done","media_ids":[...]}`) — but the import path writes a **brand-new** `job_id` and never touches the original `2037215124834251576` entry, so the Activity card kept spinning on the orphan even after the real data had landed. Two fixes, likely both needed: (1) when a task-id recovery/import succeeds for a task that has an existing non-terminal job entry, close that original entry too (a `done`/`superseded` event against `2037215124834251576`, not just the new `import-*` id); (2) a reconciliation sweep for jobs stuck at `running` past some age threshold — re-check their real PixAI status and resolve or flag them, rather than trusting the client/watcher to eventually get there. `pixai_gallery.py:12098-12114`'s comment already documents the deliberate choice not to write a false terminal state on a transient poll exception ("leave it at its last-known state... it ages out, or the live-mirror watcher collects it") — that's the right call defensively, but nothing currently makes good on the "ages out" half of that promise. |
| medium | `owner-2026-07-23` | defect | ? | **The Loom's own Activity tracker widget is functionally dead** — "shows what's active but no movement... basically dead except for making the icon and initial job" — while the exact same task, viewed from the main gallery's tracker, **does** show live progress. Same backend job data, two renderers, one works. Points at the Loom's own polling/subscription wiring for its Activity display (likely `loom/master-storyboard.jsx`'s Jobs consumer), not the shared job log or `/api/jobs` itself, since the gallery's reader of that same data is fine. Not yet root-caused — needs a look at how the Loom's tracker widget subscribes/re-renders vs. the gallery's. |
| medium | `owner-2026-07-23` | defect | ? | **No intermediate progress signal for a plain generation** — "goes right to generated and spins until done. That's it." Contrast with the Panel's job cards, which do show a real `done/total` progress bar (confirmed working in this same retest). Not yet confirmed whether PixAI's own task-status response carries anything progress-shaped for a generate task (if it genuinely doesn't, this may be a real API ceiling, not a client gap) — needs checking before scoping a fix. |
| low | `owner-2026-07-23` | defect | ? | **Failures render as the bare word "fails"**, no detail. `api_task_status()`'s failed branch (`pixai_gallery.py:12085-12087`) does forward PixAI's own status string (`st.get("status") or "failed"`) — so if the tracker is only showing the literal word "failed," either PixAI's status string is itself that terse for these cases, or something between the log write and the card's render is dropping it. Needs one real failed generation's raw job-log line to tell which. |
| low | `owner-2026-07-23` | doc-lie or defect (unconfirmed) | ? | **Edit and Reference-video submissions both show as "Edit" in the tracker**, per the owner. Code archaeology so far found two DIFFERENT hardcoded label sites — the gallery's own inline Edit tab registers `'Edited'` (`pixai_gallery.py:6784`), while both the gallery's and the Loom's shared-drawer `mg-submit` listeners register a flat `'Rendered'` regardless of tab (`pixai_gallery.py:6943`, `loom/master-storyboard.jsx:2593`) — neither matches a shared literal "Edit" bug at first read. Not resolved; needs the owner's screenshot showing exactly which tracker (gallery vs. Loom) and which two submissions to pin the real site. |
| — | `owner-2026-07-23` | design-pass material (not a defect) | 0min | Owner's own framing — bank for the design pass, not urgent: Activity/Panel status messages are too terse (a single word or a bare running-from description, no further detail); the spinner's CSS mask is "off"; spinners read as too small. Dismiss-then-reload correctly stays dismissed (confirmed working, no issue). |

178 surviving findings (plus the owner block above).
