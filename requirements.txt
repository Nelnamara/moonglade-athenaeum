requests
truststore ; python_version >= "3.10"
pillow
numpy        # moonglade_similar.py's CLIP-embedding array output (module-scope import, unlike
             # that same file's genuinely lazy heavy deps -- pixeltable/torch/transformers)
websockets   # --watch / --watch-backup, and the web gallery's auto-starting live-mirror thread

# gallery server (optional)
flask

# dev / testing
pytest
pytest-mock
pytest-cov   # coverage: python -m pytest --cov=moonglade_gallery --cov=moonglade_backup

# Optional: mDNS/Bonjour LAN discovery (the Control Panel Bonjour chip). Absent = no broadcast, app unaffected.
zeroconf>=0.130
